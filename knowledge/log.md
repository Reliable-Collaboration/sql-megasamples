# Update Log

## 2026-09-17
* **Creation**: [a live feed is taken as served](/decisions/live-artifacts.md) -- the maintainer's decision after a clean-room build from GitHub stopped on `chicago_crimes`' drifted extract: `live: true` on its 26 artifacts and on the dataset, the fetch takes what the portal serves and records it, the verifier holds the dataset to floors and structure; [the dataset record](/datasets/chicago-crimes.md) carries the observation.
* **Verification**: the live-feed path end to end in a clean-room checkout of this branch: `chicago_crimes` fetched as the portal served it on 2026-09-17 (74,889,263 bytes, 259,623 rows, against the snapshot's 74,885,948 and 259,607), built and verified on MySQL with floors, then on SQLite and PostgreSQL held to the hub's record under `build/live/` -- 0 failures on each. The first run of that test failed on the SQLite port, which had restored the previous image bake's dump of the dataset: the hub's build now re-dumps after a verified load ([the decision](/decisions/live-artifacts.md)).
* **Verification**: a fresh clone's first download crashed every fetch worker (`job.started` was never set; `fix/fetch-start-time`), found by the same clean-room build; a test now covers the progress line of a fetched job.

## 2026-09-10
* **Deviation**: [no release assets](/decisions/no-release-assets.md) -- the maintainer publishes only the repository, so the `data-v1` and `sqlite` asset staging (`megasamples/release.py`, `release/`, `make release`) and the five manifest mirrors pointing at the unpublished release are removed; a `manual` artifact's message tells the user where to download it and where to put it, and `MEGASAMPLES_ACCEPT_DRIFT=1` accepts an upstream that has moved, re-pinning the manifest. `chicago_crimes` is re-pinned to the portal's 2026-09-09 extract (259,607 rows) and verified on all three engines ([record](/datasets/chicago-crimes.md)).
* **Verification**: the SQLite service's first start was OOM-killed copying its 900 MB of files into the consoles' volume under a 64 MB limit, and its health check passed on the first file copied, before the rest were; it now copies each file under a temporary name, writes a `.complete` marker last that the health check waits for (with a start period for a slow disk), under a 256 MB limit -- `make up` then reports it healthy with all 22 files in place. Dollars in the generated command are doubled, since Compose interpolates single ones.
* **Creation**: three screenshots for the README (`docs/screenshots/`: the console index, CloudBeaver and DbGate on Sakila's `film` table), taken from the running stack by `docs/screenshots/capture.py` in the Playwright image on the stack's network (`make screenshots`), so they can be retaken after any change rather than staged.
* **Update**: the console index page opens with how to connect a tool of one's own to each engine (address, accounts, client command, URL and JDBC forms; the SQLite files and how to copy one out), lists the consoles by how many engines they browse with phpMyAdmin marked "MySQL only" and last, and the Adminer card leads to `adminer.html`, which says what its login form wants for each engine and opens it filled in; `README.md` gains the same connection table. DbGate moves to the Debian-based `dbgate/dbgate:6.6.1` image: on the Alpine one its SQLite driver's native module fails to load (`fcntl64: symbol not found`, measured), on this one it opens a file and answers; `make test-console` now checks that inside the running console, plus the connection section and `adminer.html`.
* **Verification**: [clean-room build](/runbooks/clean-room-build.md) -- the README's quick start proved from a fresh clone with the project's images, containers, volumes and build servers removed: 1.4 GB fetched in 8 minutes; `make run` built, verified and baked all 21 core datasets on MySQL, PostgreSQL and SQLite in 39 minutes with 0 failures; `make up`, `make test-console`, `make test-image` on each engine and `make check` all pass. Four defects the attempts surfaced were fixed first: a failed download stopped the whole job (the build now goes on without that dataset and names it; the fetcher falls through to the data-v1 release asset when upstream bytes are not the pinned ones -- chicago_crimes' portal extract has changed since it was pinned, which the README now says beside lahman); the catalogue needed a running image; `uv.lock` was stale; and `run` removed the build server between engines, rebuilding every dataset on MySQL once per engine (it now keeps the servers and restores from the dump). A fifth surfaced on the run after that: both build servers' readiness checks answered from the official images' first-start temporary server on the Unix socket, 109 ms before it stopped; they now ask over TCP. With all five fixed, `make run` from a wiped clone took 19 min 40 s for the three engines and every check passed.
* **Verification**: after the review fixes, all 21 core datasets re-ported to PostgreSQL through the database-keyed paths and verified (0 failures each); the PostgreSQL image rebuilt with `admin` as owner of every baked object and its tests pass, including the new check that admin alters a baked table; `make up` recreated the engine and `make test-console` passes; `make check` passes with 48 unit tests.
* **Creation**: [review dispositions, 2026-09-10](/decisions/review-2026-09-10-dispositions.md) -- the `/code-review` of the release-2 branch reported fifteen confirmed defects; each was verified against the code and fixed as a class: the oracle_oe helper's import from the deleted `scripts/`, fetch's exit code for datasets without artifacts, everything keyed by dataset name breaking `append: true` datasets (now keyed by database, with the append dataset folded into its base's registry row), a generated column's untranslatable function aborting a port after writing, the hub check that skipped an append dataset's load, the Python 3.14-only zstd import, PostgreSQL's `admin` unable to alter what it sees (it now owns the baked objects), CloudBeaver's settings tied to the MySQL engine, the landing directory created by Docker as root, the empty `SF` export, incomplete port outputs accepted by the image build, the provenance rename map read from build output, `ports-check` demanding both engines, `test-console` assuming the landing page, and connection hints hard-coding sakila. Four latent translator defects it listed past its cap were fixed too; a BIT-digest one it did not describe is open.
* **Verification**: PostgreSQL and SQLite images rebaked so their registries carry the explained reasons, `make test-image` 0 failures on both, `make up` then `make test-console` 0 failures; the `megasamples` registry inside the PostgreSQL image reports the spatial index with its reason and the pointer to the record.
* **Deviation**: the maintainer keeps the one remaining port limitation rather than adopting PostGIS for it: `sakila.address.idx_location` (the corpus's only SPATIAL index) stays unported on PostgreSQL and SQLite, with the geometry column itself carried as standard WKB. Recorded as exception 7 of [programmable-object parity](/decisions/programmable-object-parity.md); the seven kinds of exception are now also tabled in `README.md` and the reasons in every `not_ported.yaml` point at the record.

## 2026-09-09
* **Creation**: [programmable-object parity](/decisions/programmable-object-parity.md) -- views, stored routines, triggers, `ON UPDATE` columns, FULLTEXT indexes and `REGEXP` checks are ported to PostgreSQL and SQLite by deterministic translators (`megasamples/port/sqltranslate.py`, `routines.py`, `views.py`, `triggers.py`; sqlglot for statements, a scanner over a closed census for PL/pgSQL bodies, psql's `\gdesc` for a procedure's result columns, recorded once in `ports/result_sets.yaml`), and verified by execution: three new stages (`views`, `routines`, `triggers`) pin from MySQL the digest of every view (69), the printed output of 52 routine calls and of 16 trigger scenarios, and every core dataset passes on both engines. The record lists what each engine carries (PostgreSQL: 67 views, 42 routines, 11 triggers, 8 FULLTEXT as GIN; SQLite: 61 views, 10 triggers, 8 FULLTEXT as FTS5, no routines) and the seven kinds of exception with their reasons; the SPATIAL index is open with the maintainer (PostGIS, or the recorded limitation).
* **Verification**: three hub defects found by executing the ported objects, all in the T-SQL converter and reconverted: Northwind's `Invoices` added `FirstName + ' ' + LastName` as numbers; Northwind's `sales_by_year` kept `DATENAME`; AdventureWorks' two bill-of-materials procedures lost `WITH RECURSIVE` to a comment between the CTE's column list and `AS`. All three failed on MySQL itself before the fix. ([northwind](/datasets/northwind.md), [adventureworks](/datasets/adventureworks-oltp.md))
* **Verification**: images rebaked for all 21 core datasets with the ported objects and the corrected dumps, and tested (`make test-image`: MySQL 1,783 MB data directory, 3.46 GB image; PostgreSQL 2,612 MB data directory, 4.07 GB image; SQLite 899 MB of files, 1.26 GB image); `make up` with the three engines and four consoles, then `make test-console`: 0 failures; `make check` (bundle, generated files, port records reproducible for 21 datasets, 44 unit tests): 0 failures.
* **Update**: [engine hub](/decisions/engine-hub.md) outcome names what is now ported and points at the parity record; `README.md`, `ARCHITECTURE.md` (4.4 the port toolkit, section 5 stages S4v, S8r, S9) and `PLAN.md` describe the state.
* **Verification**: [docker multi-stage record](/tools/docker-build-multistage.md) -- a recreated engine container keeps the anonymous volume the official image declares for its data directory, so a rebuilt image's data was shadowed by the old cluster in the running stack; `megasamples up` recreates such an engine with `--renew-anon-volumes`. The BuildKit cache was suspected first and was not the cause.
* **Verification**: images for all 21 core datasets built and tested on every engine on 2026-09-09: `sql-megasamples-mysql:dev` rebaked from the corrected dumps (66 checks, adventureworks now holds its 16 routines); `sql-megasamples-postgres:dev` (40 checks, ready in 0.5 s, 2,549 MB data directory, 3.99 GB image); `sql-megasamples-sqlite:dev` (integrity, foreign-key and journal checks on every file, 867 MB of files, SQLite 3.49.2, 1.21 GB image). `make up` with all three engines and the four consoles, then `make test-console`: every console answers, the landing page names all 21 databases of each engine, CloudBeaver opens each engine's connections.
* **Creation**: [PostgreSQL image shape](/decisions/postgres-image-shape.md).
* **Update**: [CloudBeaver unattended startup](/runbooks/cloudbeaver-unattended-startup.md) records how the SQLite driver is enabled and the driver ids that connected.
* **Update**: `README.md`, `ARCHITECTURE.md` and `PLAN.md` describe the three-engine state; the catalogue carries an engines column read from each dataset's `ports/not_ported.yaml`; `make release SET=sqlite` stages the SQLite files as an asset set.
* **Creation**: [PostgreSQL official image](/tools/postgres-docker-official-image.md), [target PostgreSQL version](/decisions/target-postgres-version.md), [MySQL Shell dump format](/tools/mysql-shell-dump-format.md), [SQLite file conventions](/decisions/sqlite-file-conventions.md), and the three docker-library source records they cite.
* **Verification**: the port toolkit (`megasamples/port/`: model from information_schema, one type map, one DDL emitter for two dialects, the dump reader) built every core dataset on PostgreSQL 18.6 and as SQLite files, and `megasamples verify --engine postgres|sqlite` passed counts, canonical digests, foreign-key orphan checks and index parity for all 21 on each -- the same `tests/` files MySQL is checked against.
* **Update**: [engine hub](/decisions/engine-hub.md) gained the rules verification forced: identity columns keep an integer type, names over 63 bytes are shortened by a shared function, generated columns are recomputed by the target, geometry is standard WKB. [Checksum method](/decisions/test-checksum-method.md): the geometry rendering is `ST_AsBinary(col, 'axis-order=long-lat')`; `adventureworks` digests re-pinned from MySQL (only `person_address` moved).
* **Verification**: the release-1 MySQL image holds none of `adventureworks`'s 16 routines: its dump was taken before the converter produced them, while the pinned smoke expectation (`13 16 0` views, routines, triggers) and the staged SQL both carry them. Reconverting (`make adventureworks`) gives 16 routines and a clean verification; the fresh dump records 10 functions and 6 procedures. The other 20 core datasets' staged SQL and dumps agree on every view, routine and trigger count.
* **Update**: every port now writes `datasets/<name>/ports/{postgres,sqlite}.sql` and `not_ported.yaml`, and `megasamples ports-check` regenerates them and fails on any byte of difference, so the ports are reproducible by construction and reviewed as diffs.
* **Creation**: [engine hub](/decisions/engine-hub.md) — MySQL is the hub and PostgreSQL and SQLite are ports of the verified corpus; with the measured inventory of what the ports must translate (21 databases, 248 tables, read from the built image's information_schema).
* **Creation**: [stack configuration](/decisions/stack-configuration.md) — `megasamples.yaml`, the interactive configurator, and `compose.yaml` generated from the configuration.
* **Update**: [repository layout](/decisions/repository-layout.md) and [build orchestration](/decisions/build-orchestration.md) rewritten for the package layout: `megasamples/` (pipeline, `engines/mysql/`, `sources/`), `engines/<engine>/`, `consoles/<console>/`, `build/stage/` and `build/mysql/`; the repository renamed to sql-megasamples and every reference outside the dated source records updated; dataset contracts gained a `stage:` block and a `quick` flag and lost `converter: none`.
* **Verification**: after the restructure, `make sakila` and `make chinook` pass every stage (counts, digests, foreign keys, indexes, explain, smoke); `make image FROM_DUMPS=1` baked `sql-megasamples-mysql:dev` from the 21 existing dumps in 1 min 40 s; `make test-image` passed 62 checks over 22 databases (1,785 MB data directory); `make up` started the generated stack and `make test-console` passed its 9 checks; `make fetch` re-downloaded `sakila/sakila-db.zip` (729,654 bytes, sha256 verified) through the new monitor.
* **Update**: [bundle index](/index.md) intro names the three engines.

## 2026-09-04
* **Deviation**: the project's code licence changes from **MIT to Apache-2.0** at the user's direction, and the repository gains a root `LICENSE`, which it never had — the MIT claim lived only in prose. The change is deliberately narrow: Apache-2.0 covers the scripts, converters, tests, Dockerfiles, knowledge bundle and generated documentation, and **covers none of the data**. It is not a relicensing of anything upstream and could not be — `employees`, `lahman`, the Stack Exchange datasets and `wikipedia_simple` are share-alike and remain CC BY-SA, which the CC BY-SA record had already said the repository licence must carve out. `LICENSES.md`, `NOTICE.md` and the README now say so explicitly, and `NOTICE.md` gained an Apache §4(d) notice block kept visibly separate from the data attribution it already carried, because the two are different obligations and satisfying one does not satisfy the other.
* **Creation**: a one-line description of every database now appears in `README.md`, `CATALOGUE.md` and on the console landing page, and it is **not written in any of them** — it comes from the dataset's own knowledge record, trimmed by one shared rule in `megasamples/catalogue.py` that `megasamples/console_page.py` imports, so the three cannot describe the same dataset differently and none can drift from the research. The trimming took three corrections to get right: cutting at `". "` truncated "11,140 rows incl. 8,640 multilingual products" to a sentence ending in "incl", and the length ceiling cut inside parentheses and left them hanging open. `dataset.yaml` may carry a `blurb:` that wins over the record, used once so far — smallsets' record describes which naming option was accepted, which means nothing to a visitor. The README block is spliced between markers and `make check` fails if it is stale.
* **Creation**: each row of the console landing page carries **P** and **A** links that open that one database directly in phpMyAdmin and Adminer. Only those two accept a database in a URL, which was measured rather than assumed: DbGate's front-end bundle reads only auth parameters (`code`, `state`, `sid`) and CloudBeaver's carries no routing parameters at all, so neither gets a per-row link rather than one that would quietly open its front page. Both working links were followed end to end — phpMyAdmin lands on `/ sakila`, Adminer on `Database: sakila` once past its login form.
* **Verification**: **V-01 is done, and its deferral was based on a wrong premise.** `make verify-oracle` loads HR, CO and SH into Oracle AI Database 26ai Free from the same upstream scripts and compares them against MySQL: **575 aggregates over 30 objects, 0 differences** — row counts, per-column non-null counts, exact-numeric sums, summed string lengths and date ranges, for tables *and* views. The deferral said it needed SQLcl, which carries an OTN licence; it does not. The Oracle Free image ships **SQL*Loader**, which loads the six SH CSVs that `sh_populate.sql` expects SQLcl's `LOAD` command to handle, so the only licence involved is the Oracle Free Use Terms for the database image — printed by the script, which refuses to start until they are accepted. Nothing Oracle produced is redistributed and the container is removed at the end.
* **Update**: three things had to be got right before that number meant anything, and each was a real defect in the first attempt. The Oracle user must be named for the schema (`SH`, not `SHX`), because `sh_create.sql` qualifies its materialized-view query as `sh.sales` — under any other name the two materialized views fail with ORA-00942 and quietly do not exist, and MySQL has both as views. `ENABLE QUERY REWRITE` needs its own grant. And both materialized views and the statistics have to be built **after** the CSVs are loaded: the upstream script creates the views and gathers statistics before the `LOAD` lines, so left alone Oracle ends up with empty materialized views and an optimizer that thinks the schema is empty. `store_orders` is the one object absent from MySQL, which M-02 already recorded — it needs `GROUPING SETS`, which P-02 verified is HeatWave-only on 9.7 — so the script carries it as a declared exception with that reason rather than reporting it as a difference.
* **Deviation**: the comparison computes every metric for an object in **one** pass. The first version issued one aggregate per column and stitched them with `UNION ALL`, which scanned the object once per column: SH's `profits` view takes ~45 s to scan and has 10 columns, so a 45-second check became a 15-minute one that then reported *itself* as "too slow to compare". The reported limitation was an artefact of the measuring tool, not of either database.
* **Verification**: three open questions are closed with answers the project's own work already contained but had never written down. **TPC-C determinism**: no — same seed, one thread, and eight of nine tables differ, because the Lua populator does not thread the seed through row generation; the consequence was taken rather than worked around, which is why that dataset alone carries no content digests. Debian's sysbench does authenticate to MySQL 9.7 with `caching_sha2_password`, so that concern was unfounded. **Stack Exchange 2024-04 row counts**: counted and pinned for both dumps — 3,035,686 rows for `dba`, and the beer figures beside them — and fixed for good rather than merely current, since the archive.org snapshot's md5 matches what the record pins. **An official OKF validator**: none exists; the format's repository ships no validation entry point, so `megasamples/okf_check.py` is the authority for this bundle and the record now says so instead of implying an external one. The other 26 questions stay open on purpose: five are licensing readings recorded in the README, and the rest need an upstream fetch or a benchmark run that nothing currently depends on.
* **Creation**: **R-02, the last task: the `data-v1` release is prepared and deliberately not published.** `make release` stages five assets with `SHA256SUMS` and a `MANIFEST.md` explaining why each one is there — an asset earns its place only by being something a third party cannot otherwise obtain and check: the lahman zip (SABR share link, no static URL), the Chicago 2024 snapshot and its IUCR codes (the portal's data changes daily), and the two Stack Exchange archives (the archive.org item could be taken down). Every staged digest matches `manifest.yaml`. Everything else in `downloads/` has a stable URL with a pinned digest, so mirroring it would add bytes and no verifiability. `megasamples/release.py` refuses to stage Citi Bike, Divvy or TPC data whatever its own asset list says. **No release was created, nothing was uploaded, nothing was made public** — that is the maintainer's step (the staged manifest, removed on 2026-09-10 with the decision to publish no release assets).
* **Verification**: the §8.3 pre-publication checklist is now `megasamples/prepub.py` and all ten items pass, with `megasamples/audit.py` owning items 4–6. It is written to be re-run on the day of publication rather than only the day of preparation. `make check` gained `CATALOGUE.md`, a generated per-dataset catalogue with what each licence asks of a redistributor.
* **Update**: three defects surfaced from running the checklist rather than reading it. **The image carried no attribution at all** — several licences require notices inside any redistribution and an image is one; `NOTICE.md`, `LICENSES.md`, `CATALOGUE.md`, `README.md` and all 76 per-dataset licence and provenance files are now baked at `/usr/share/doc/megasamples`. **`enron_full` and `wikipedia_simple_full` pinned nothing**: `gen_provenance.py` grouped artifacts by the manifest's owning dataset, so a dataset built from another's download got an empty table under the sentence "every byte this database is built from, pinned"; artifacts are now the union of owned and declared. **The generated datasets made the same false claim** with nothing to pin at all — `ssb`, `tpch` and `tpcds` now state that nothing is downloaded and that what is pinned is the generator's commit.
* **Deviation**: `CATALOGUE.md` has no size column. The first version measured `information_schema`, which reports a different `data_length` after a restart — `adventureworks` moved 119 → 124 MB with no data change — so a generated file checked by `make check` would have failed at random. Table and row counts come from the registry, which is baked at build time and does not move.
* **Update**: `README.md` is rewritten for someone who has never seen this repository: what it is (21 databases, 248 tables, 9,056,697 rows, ~3.5 GB image), what they need, how to build it, how to bring the stack up and down, where the console index is, both accounts and how to change their passwords, and the licensing obligations that travel with the data. Every figure in it was measured against the running image rather than carried over from the plan, every `make` target it names was checked to exist, and its run instructions were followed end to end — including connecting to the published `127.0.0.1:3306` as each account, which confirmed `demo` is refused writes from outside the container too. It also documents the two things a newcomer would otherwise hit blind: `lahman` cannot be fetched by a build (SABR share link) and needs supplying by hand, and the shorter fifteen-dataset build is 203 MB rather than 1.4 GB.
* **Update**: `make image` now **removes the build containers when the build finishes**, at the user's direction — the build server holds a second copy of every dataset and had been left up for 28 hours. `KEEP_BUILD_RESOURCES=1` keeps them, which is what a converter developer wants, since the next `make <dataset>` then reuses the loaded data instead of reloading it. `megasamples/sources/wwi_export.py` gained the same discipline: it removed its SQL Server only on the success path, so a failed export left an EULA-licensed container running indefinitely; it is now in a `try/finally`.
* **Deviation**: the `console` Compose profile is **removed**, at the user's direction, and §12 is updated to match. The database and the four consoles are persistent services and now come up and go down as one project — `make up` / `make down`, or plain `docker compose up -d` / `down`; `docker compose up -d mysql` still starts the database alone, which is what the profile was actually protecting. The published image is unchanged either way. The split the profile drew was the wrong one: what matters is **persistent versus transient**, not console versus database ([decision](/decisions/browsing-console-stack.md)).
* **Update**: every container this repository starts outside the stack is now labelled `megasamples.transient=true` with a `megasamples.role` — the `megasamples-build-mysql` server, the SQL Server behind `make wwi-export`, the loader, the servers the tests start. `megasamples/workspace.py` lists them beside the stack (`make status`) and removes them (`make clean`, `make clean-all` to take the stack down too); it also matches the known names, so a container started before the label existed still cleans. Nothing removes the build server automatically: `docker rm` discards every dataset loaded into it and reloading takes hours, so it is a build session's workspace and `make clean` is how a session ends. Found while checking: an `megasamples-build-mysql` had been up for 28 hours with all 21 databases in it, and three CloudBeaver probe containers had survived a cleanup that used the wrong names.
* **Update**: both accounts are now reachable from every console, not just the read-only one. phpMyAdmin gets two servers from a mounted `config.user.inc.php` (its image forces one account across every server it generates, which is why the file exists), DbGate two `CONNECTIONS`, CloudBeaver two seeded connections, and Adminer's login form takes either. Each console opens on `demo`, so the safe account is the one you get without choosing. Passwords come from `.env` — `DEMO_PASSWORD` and `ADMIN_PASSWORD`, the names the image's entrypoint already applies with `ALTER USER` — and compose passes the same value to the server and to every console; verified by running the whole stack under changed passwords, where the old ones stopped working and all four consoles followed ([decision](/decisions/browsing-console-stack.md)).
* **Verification**: a real defect surfaced doing it. CloudBeaver's `admin` connection failed with `Public Key Retrieval is not allowed` while `demo` worked — not a wrong password, but MySQL's `caching_sha2_password` full-auth path: the JDBC driver refuses to fetch the server's RSA key over an insecure connection, and `demo` only appeared to work because the healthcheck keeps its credential cache warm. Fixed with `allowPublicKeyRetrieval` in the seeded connection, and confirmed against a cache cleared by `FLUSH PRIVILEGES`. DbGate's `mysql2` and phpMyAdmin's `mysqli` both handle the cold path themselves — tested, not assumed. S10 now opens each CloudBeaver connection rather than only listing it, because listing is what succeeded while connecting failed.
* **Creation**: C-01, the browsing console. `docker compose --profile console up -d` starts a generated landing page on 8080 alongside phpMyAdmin, Adminer, DbGate and CloudBeaver, every port bound to `127.0.0.1` and every console connected as the read-only `demo` account. Without the profile, `docker compose up` starts only the database and the published image is unchanged. `megasamples/console_test.py` is S10: five endpoints answering 200, every registry name on the page, the credentials on it, and CloudBeaver asserted to be out of configuration mode and showing the connection anonymously — HTTP 200 does not distinguish a working console from a wizard ([decision](/decisions/browsing-console-stack.md)).
* **Verification**: **CloudBeaver ships after all, and the deviation below is retracted.** It does start unattended; the earlier conclusion was drawn from an incomplete search, not from a limitation of the product. Three things are needed, each silently insufficient alone: a `java.util.Properties` file at exactly `conf/.cloudbeaver.auto.conf`, whose *presence* makes `CBApplication.performAutoConfiguration` finish the setup instead of showing the wizard (environment variables alone never do it, and the same file at two other plausible paths is ignored); the connection in `conf/initial-data-sources.conf`, which the image's own entrypoint copies into the workspace; and `CLOUDBEAVER_APP_GRANT_CONNECTIONS_ACCESS_TO_ANONYMOUS_TEAM=true`, without which the wizard is gone, an administrator can see the connection, and an anonymous visitor still gets an empty sidebar. Verified end to end on the shipped compose stack: an anonymous session lists the connection, connects, and returns `SELECT COUNT(*) FROM sakila.film` = 1,000 ([runbook](/runbooks/cloudbeaver-unattended-startup.md)).
* **Update**: two API details cost most of that investigation and are now written down. CloudBeaver declares `configureServer`, `authLogin` and `addConnectionsAccess` as **`Query`** fields, so calling them as mutations returns `FieldUndefined` — which reads exactly like a missing feature and was misread as one. And local passwords arrive **pre-hashed**: `SecurityUtils.makeDigest` is MD5, hex, upper-cased, so a raw password over the API always fails while the same password typed into the browser works ([runbook](/runbooks/cloudbeaver-unattended-startup.md)).
* **Deviation**: *retracted — see the Verification entry above; this conclusion was wrong.* **CloudBeaver is dropped**, on the rule the decision itself set — a console that cannot start unattended ships nothing rather than shipping a manual step. Version 25.2.0 stays in `configurationMode: true` however it is configured: a mounted `initial-data-sources.conf` carrying the connection, `CLOUDBEAVER_APP_ANONYMOUS_ACCESS_ENABLED=true`, and both `CB_ADMIN_NAME`/`CB_ADMIN_PASSWORD` and `CLOUDBEAVER_ADMIN_NAME`/`CLOUDBEAVER_ADMIN_PASSWORD` were each tried, and its GraphQL `serverConfig` reported the wizard every time.
* **Verification**: the other three preconfiguration questions are answered by measurement. DbGate's engine string really is `mysql@dbgate-plugin-mysql` — its own log prints it, where the record could only infer it from an mssql example. Adminer's login form really does remain with `ADMINER_DEFAULT_SERVER` set, so the landing page states the credentials and S10 asserts they are there. And phpMyAdmin signs in from `PMA_USER`/`PMA_PASSWORD` and opens straight into the data ([question](/questions/console-preconfiguration-limits.md)).
* **Update**: the landing page is generated by `megasamples/console_page.py` from `megasamples.datasets` inside the running image — the same table the image test asserts against — so it lists the **21 databases actually present, 248 tables and 9,056,697 rows**, with each one's tier, size and licence. A hand-written page would have been wrong the first time a dataset moved.
* **Deviation**: V-01, the Oracle cross-check, is deferred rather than done, and the plan already marks it optional. It needs the 3.7 GB Oracle Free image *and* SQLcl, which is in neither image and carries its own OTN licence — the SH installer needs SQLcl's `LOAD` command. The three Oracle converters already verify against the row counts the research recorded, so the cross-check would confirm what is confirmed rather than close a gap. It is worth doing if those schemas are ever changed.
* **Verification**: B-04 complete. SSB at SF 1 is **5 tables, 6,235,730 rows, 1,272.5 MB**, and it answers the [row-count question](/questions/ssb-sf1-row-counts.md): every cardinality rule in the paper holds exactly, supplier at **2,000×SF** rather than the 10,000×SF older forks used. Building the generator with `EOL_HANDLING=ON` and `YMD_DASH_DATE=ON` is what makes the `.tbl` files load with no rewriting at all — the trailing pipe and the integer dates would both otherwise need a pass ([record](/datasets/ssb.md)).
* **Verification**: B-03 complete, and both of the decision's open execution questions are answered. sysbench **does** authenticate to MySQL 9.7 — the `caching_sha2_password` concern was unfounded, and Debian's MariaDB client handles it too. The determinism experiment (risk 10) says **no**: two loads with the same `--rand-seed` and one thread produce different data in **eight of the nine tables**, only `new_orders` matching. sysbench-tpcc does not thread its seed through row generation ([record](/datasets/tpc-c.md)).
* **Deviation**: TPC-C is therefore the one dataset in the project that cannot carry pinned digests. Its counts are stable — seven of nine are exactly the specification's W=1 cardinalities — so `megasamples/sources/tpcc_load.py` asserts those and says plainly that content is not pinned, rather than pinning a checksum that would fail on the next load. `order_line` is excluded even from the count check, because TPC-C specifies 5 to 15 lines per order at random; it came out at 299,674 and 300,222 on two runs.
* **Creation**: `engines/mysql/loader.Dockerfile` (task P-04), built at the point something needed it rather than as scaffolding — SSB has to compile dbgen from source and TPC-C needs sysbench, and neither gcc nor sysbench is on this host. It carries gcc, cmake, sysbench 1.0.20, ssb-dbgen and sysbench-tpcc, each pinned by commit, and nothing from it reaches the published MySQL image.
* **Deviation**: no host installation was needed after all, which is worth recording because the obvious move was to ask for one. The Docker daemon still cannot pull from Docker Hub — the blob CDN answers with a bare EOF, the same IPv6 signature as before — but **container networking is fine**: apt and git both work from inside a container. So `megasamples/pull_image.py` fetches the base image over IPv4 and the Dockerfile refers to it by digest. `pull_image.py` gained Docker Hub's shorthands (`debian:12-slim` means `registry-1.docker.io/library/debian`), which it had not handled.
* **Verification**: B-02 complete. TPC-DS at SF 1 is **24 tables and 19,557,579 rows in 5,292.7 MB** with 77 secondary indexes, `store_sales` at the specification's 2,880,404. dsdgen emits no constraints at all, so the 24 primary keys come from the specification and are checked against the generated data — for NULLs and for uniqueness — before being declared; all 24 hold ([record](/datasets/tpc-ds.md)).
* **Deviation**: **DuckDB 1.5.5 ships no usable TPC-DS answers.** `tpcds_answers()` reports "Don't have TPC-DS answers for SF 10.000000" whatever has been generated, so the specification's validation output is not available the way TPC-H's is. The 99 queries are compared against DuckDB's own results on the same generated rows instead — two independently written engines over identical data, which catches a mistranslation but is **not** the specification's answer set. The record says so plainly rather than letting "99 queries checked" imply more than it is.
* **Verification**: against that reference, with a 60-second limit per query, **48 of 99 match exactly and 0 differ**; 51 exceed the limit. The 51 are a fact about MySQL at this scale — TPC-DS is join- and aggregation-heavy and MySQL executes it on one core — not about the port.
* **Deviation**: adding 77 secondary indexes did **not** change that count, and the detail is more interesting than the total. Before them 51 queries were too slow; after them 51 still were — but not the same 51. Q09 became fast and Q31 became slow. The indexes are kept because they make the join columns reachable and the fact tables usable, not because they rescue the benchmark. Claiming "the indexes made no difference" would have been wrong in both directions.
* **Update**: two comparison rules were needed, both about representation rather than arithmetic. `AVG` over a `DECIMAL(7,2)` column returns `15.3333` in MySQL where DuckDB returns `15.333333333333334`, so values are now compared at the precision of the less precise side — a flat relative tolerance called two correct answers wrong. And an empty field from dsdgen must load as NULL rather than the empty string, because the queries test optional foreign keys with `IS NULL`.
* **Update**: `megasamples/sources/tpc_check.py` takes a per-query time limit, so the check is bounded and repeatable rather than open-ended. A query that exceeds it is reported as too slow, which is a fact worth recording, instead of stalling the run — the first attempt had a single query executing for ten minutes.
* **Verification**: B-01 complete, and the part that matters is not the load but the answers. TPC-H at SF 1 is **8,661,245 rows in 1,872.7 MB**, `lineitem` at the specification's 6,001,215 — and **all 22 queries return the reference answers**. `megasamples/sources/tpc_check.py` compares each against DuckDB's `tpch_answers()`, which carries the spec's validation output; 22/22 match at SF 1. Q1's four rows come out exactly as the spec prints them ([record](/datasets/tpc-h.md)).
* **Verification**: two schema decisions turned out to be load-bearing rather than cosmetic. **`utf8mb4_bin`** on every table, because the reference answers order strings by byte value and MySQL's default case-insensitive collation reorders ties in several queries — the rows are individually right and the comparison still fails. And **`DECIMAL(15,2)` taken from `dss.ddl`** rather than from the generator's inferred types, because the answers are decimal sums to the cent.
* **Deviation**: the query rewrites the plan anticipated do not fire. DuckDB's `tpch_queries()` already writes `CAST('1998-09-02' AS date)` where dbgen writes `date '1998-12-01' - interval '90' day (3)`, and MySQL accepts all 22 unchanged. The rules stay in the converter because a different generator version may need them, but the record now says they were not needed here rather than implying they were.
* **Update**: the comparison is numeric with a relative tolerance, not textual. MySQL prints `37734107.00` from a `DECIMAL(15,2)` sum where the reference prints `37734107`, and the two engines carry different intermediate precision in the averages; string equality would fail on correct output.
* **Update**: the two TPC licence questions need no answer to proceed. Both already have a conservative default recorded and adopted — ship no generated data, and write the query texts into the staging directory at build time rather than committing them — and neither costs anything here, since all four benchmark datasets are generated on demand anyway. The questions stay open because a reply from TPC would let the project publish a small pre-generated scale factor, not because anything is blocked ([data](/questions/tpc-eula-generated-data-redistribution.md), [query text](/questions/tpc-query-text-redistribution.md)).
* **Creation**: `megasamples/sources/bikeshare.py` (task X-04) loads Citi Bike or Divvy from one code path, and **refuses to download until the licence is accepted explicitly** — the same shape of gate as the SQL Server EULA. These are the only two datasets whose data the project never redistributes: the build fetches nothing, `manifest.yaml` lists nothing for them, no release asset exists, and the download happens on the machine of whoever accepts. Verified end to end: **25,809 Citi Bike trips in 12.1 MB** and **84,776 Divvy trips in 37.1 MB**, both within a hundred of the counts the records inferred ([citibike](/datasets/citibike.md), [divvy](/datasets/divvy.md)).
* **Verification**: the records' warning that a downloader "must not template blindly" is exact, and now checked against the live buckets — 174 Citi Bike keys and 95 Divvy keys, matching what the records recorded. Listing and matching resolves `JC-201708 citibike-tripdata.csv.zip` (a space), `JC-202207-citbike-tripdata.csv.zip` (upstream's typo), `JC-202510-citibike-tripdata.zip` (no `.csv` infix) and `Divvy_Trips_2015-Q1Q2.zip` (a hyphen its siblings do not use). A constructed name finds none of the four.
* **Verification**: the archive traps are handled and tested — multi-part *stored* CSVs in one zip, yearly archives that are zips of zips, and `__MACOSX/._*` members throughout. A pre-2021 file is **refused rather than half-loaded**: the 15-column legacy layout has no `ride_id` and is a different table, not a spelling variation.
* **Update**: `megasamples/provenance.py` tells the truth for the `not-shipped` tier. It was printing "Every byte this database is built from, pinned. `megasamples/fetch.py` refuses to proceed…" over an empty artifact table, which is exactly backwards for a dataset the build never downloads.
* **Verification**: X-05 complete — three larger tiers of datasets the image already ships small. **`stackexchange_dba`**: 11 tables, 3,035,686 rows, 1,751.7 MB. **`enron_full`**: all 150 mailboxes, 517,401 messages and 4,254,342 recipients, 2,373.8 MB. **`wikipedia_simple_full`**: 284,749 articles and 18,899,074 pagelinks, 23,438,218 rows, 3,369.4 MB. Each is a separate database rather than an append, because each is the same schema with more rows ([stackexchange](/datasets/stackexchange.md), [enron](/datasets/enron.md), [wikipedia-simple](/datasets/wikipedia-simple.md)).
* **Deviation**: `email`'s `compat32` policy does not always return a string. For a header carrying an encoded word or raw 8-bit bytes it returns an `email.header.Header`, and the full Enron corpus has them — none of the five core mailboxes does, so it surfaced only as an `AttributeError` on `.replace` deep inside recipient parsing. Every header now goes through one coercion, not just the one that crashed.
* **Verification**: the Enron header question, answered over the whole corpus rather than five mailboxes: **0** messages need the cp1252 fallback, **0** have an unparseable `Date`, and all **517,401** `Message-ID` values are distinct, so the declared `UNIQUE(message_id)` holds at full scale. 30 messages carry a header defect. Dates run **1979-12-31 to 2044-01-04** and the raw headers say so verbatim; 1,042 messages fall outside 1998–2002, which is the record's "a few bogus years" as a number.
* **Verification**: **every Enron message's Cc and Bcc address sets are identical** — 0 exceptions in 517,401 messages, and the counts match exactly at 562,364 each. That is a property of how CMU assembled the corpus rather than of the conversion, and it is worth knowing before anyone reads meaning into the Bcc column.
* **Verification**: dba.stackexchange.com spans **all three CC BY-SA versions** — 1,828 posts at 2.5, 141,720 at 3.0, 99,862 at 4.0 — where the core `beer` site's sit in one. The per-row `ContentLicense` is what makes attribution possible, and holding both tiers is what makes that checkable rather than asserted.
* **Update**: three converters gained a mode rather than a copy. `stackexchange_beer/convert.py` takes `--site <slug>` (every Stack Exchange dump has the same eight XML files), `enron/convert.py` takes `--full` (the mailbox budget becomes unlimited), and `wikipedia_simple/convert.py` takes `--full` (the 5,000-article sample becomes every ns0 page). Each core dataset was re-converted, re-loaded and re-verified to prove the refactor changed nothing.
* **Creation**: `megasamples/sources/tsqlbody.py`, the T-SQL routine-body translator (task V-02). Of the **47 routines** across AdventureWorks, AdventureWorks DW, Northwind and pubs, **30 are translated and accepted by MySQL and 17 are refused with a stated reason**; there are no failures in between, and nothing is emitted on a guess. Verified by calling them rather than by their creating: `ufnLeadingZeros(42)` is `00000042`, `ufnGetStock(1)` is 324, product 707's dealer price is exactly 60% of its list price, and `vTimeSeries` returns its 100 rows ([OLTP](/datasets/adventureworks-oltp.md), [DW](/datasets/adventureworks-dw.md)).
* **Deviation**: `sqlglot` was the obvious prior work -- it is already in this project's stack -- and it was tried first and rejected on evidence. On the simplest routine in AdventureWorks, a twelve-line scalar function, it dropped the `RETURNS int` type, turned `IF (@ret IS NULL) SET @ret = 0` into an **empty string**, and parsed `RETURN @ret` as a column alias, while printing `Unsupported If block syntax` and emitting confident-looking output anyway. Its own record already says why: "SQLGlot is a transpiler, not a validator." It stays in use for statements and queries, where it works.
* **Verification**: what makes a hand-written translator tractable here is that the census is closed. Across those 47 routines there are **no** WHILE loops, no cursors, no table variables, no dynamic SQL, no MERGE and no GOTO — the constructs that make general T-SQL translation hard. Eleven mechanical rewrites over 1,276 lines cover the rest. WideWorldImporters is the opposite case and is mostly refused rather than translated: 57 of its 72 routines are SQL Server feature configuration, SSIS ETL or the randomised data generator, and 69 of the 72 contain DDL.
* **Deviation**: three of AdventureWorks DW's five views and two of its three functions were being **lost silently**, and had been since M-03. The install script puts several comment lines above each object, and `split_statements` stripped only one before deciding whether a batch was a routine — so those bodies were split on their own semicolons and the `CREATE` ended at `AS`. The splitter now stops splitting once a routine has begun. The DW record's "4 of the 5 views are created" was measuring the bug, not a MySQL limitation.
* **Deviation**: a 1000× error in shared code. `DATE_PARTS` mapped T-SQL's `ms` to `MICROSECOND`, so `DATEADD(ms, -2, x)` meant two microseconds rather than two milliseconds. MySQL has no `MILLISECOND` interval unit, so it is now `INTERVAL (n) * 1000 MICROSECOND`. No shipped SQL used it yet; `ufnGetAccountingEndDate` would have.
* **Deviation**: `convert_tsql_convert` split its argument list on the first comma, so a three-argument `CONVERT(datetime, '20040701', 112)` became `CAST( '20040701', 112 AS DATETIME)` — a stray comma and an unbalanced paren. It now handles the six date styles the sources actually use (107, 111, 112, 113, 121, 126) and leaves an unknown style as a `CONVERT` so it fails at CREATE time rather than silently. Nineteen occurrences across all sources; none had reached shipped SQL.
* **Deviation**: `convert_concat` swallowed a cast's target type — `CAST('0' + CAST(n AS CHAR) AS CHAR(2))` split on `+` into `'0'` and `CAST(n AS CHAR) AS CHAR(2)` and joined them with `CONCAT`, taking the outer `AS CHAR(2)` inside. It now separates a trailing `AS <type>` before concatenating, and it takes the character-typed locals as a second tell so that `REPLICATE('0', n) + @Value`, which has no string literal at the top level, is still recognised as concatenation.
* **Update**: two conventions the translator makes visible rather than hidden. `@Local` becomes `v_local` and `@Param` becomes `p_param`, because MySQL has no sigil and a local named `Name` inside a routine that also selects a `Name` column resolves to the column. And T-SQL scatters `DECLARE` through a body where MySQL wants every one at the top of its block, so declarations are hoisted and an initialiser that cannot travel with them stays behind as a `SET`.
* **Update**: T-SQL `datetime` keeps milliseconds and these routines rely on it — `ufnGetAccountingEndDate` is `DATEADD(ms, -2, ...)`, which a MySQL `DATETIME` with no fractional digits rounds back up to the next day. Routine parameter, local and return types widen to `DATETIME(3)`; columns are unchanged.

## 2026-09-03
* **Verification**: X-03 complete — five extended-tier datasets, none of them baked into the image. **nyc_taxi yellow**: 3,475,226 trips appended in 22 s, taking `nyc_taxi` from 11.1 MB to 610.9 MB; the DECIMAL(10,2) narrowing is lossless (no money value in the file needs a third decimal, which the converter now checks) and MySQL's sums match DuckDB's over the Parquet exactly. **bts_ontime**: 539,747 flight segments × 109 columns in 210.1 MB. **chicago full**: 8,240,594 crimes for 2001–2024, taking `chicago_crimes` to 2,031.9 MB. **contoso**: 5,890,897 rows / 767.3 MB at 1 M orders and 59,114,388 rows / 7,766.9 MB at 10 M ([nyc-tlc](/datasets/nyc-tlc.md), [bts-ontime](/datasets/bts-ontime.md), [chicago-crimes](/datasets/chicago-crimes.md), [contoso](/datasets/contoso.md)).
* **Verification**: every one of BTS's eleven recorded hazards is real, checked against the data rather than the readme — the trailing comma that makes the header 110 fields, `"2400"` in the actual-time columns (33 `DepTime`, 245 `ArrTime`, 58 `WheelsOff`, 202 `WheelsOn`, and **never** in a scheduled column), `FlightDate` as `2025-01-01` against a readme that says `yyyymmdd`, integers written as decimals, two null spellings, leading-zero strings, and `Div3..Div5` holding **nothing at all** across 32 columns. Two corrections: hazard 3 needs no special case, because MySQL's `TIME` runs to 838:59:59 and `MAKETIME(24,0,0)` is simply `24:00:00`; and the `--ipv4` the record recommends is unnecessary, since `transtats.bts.gov` has no AAAA record.
* **Deviation**: the record inferred BTS's lookup files were ASCII-transliterated and asked for a re-check. They are not. **One byte in 319,686** of `L_AIRPORT.csv` is `0xCD` — the `Í` of `Rio Hato, Panama: Scarlett MartÍnez International` — which makes the file Latin-1 and makes a UTF-8 read of it fail outright. The converter decodes that one file as cp1252 and reports how many bytes it re-encoded, so a future month that grows more of them says so.
* **Verification**: Chicago's hazard 4 measured rather than routed around. The SODA CSV carries `location` with embedded newlines, so one year is 1,451,731 physical lines for 485,974 rows; DuckDB reads it correctly and the column is dropped in the projection. Asking SODA to omit it with `$select` **triples** the request time — 21 s to 67 s for a single year — which is the opposite of what the obvious fix suggests.
* **Verification**: `id` is unique across all 8,240,594 Chicago rows and `case_number` is not (8,240,009 distinct, 585 repeats), confirming hazard 5 at archive scale; every IUCR code in 24 years resolves against the current 434-row lookup, so the foreign key is declared. The core `crimes` table is a strict subset of the archive: 259,268 rows on both sides and every id present in both. Published null counts reproduce, `district` at exactly the published 47.
* **Deviation**: I claimed in a manifest comment that Chicago's 2024 had drifted from 259,267 to 259,268 rows. It had not — the core dataset pinned 259,268, and the file fetched for the archive came back **byte-identical** to the one the core already had, through a different `$limit`. The comment is corrected to say what was actually measured: the portal revises historical records (`updated_on` runs to the day before this build), so digests are not guaranteed over months, but over minutes the source is stable.
* **Verification**: what scales in Contoso is not what "1 M orders" suggests. `customer` is 104,990 at **both** 100 k and 1 M, along with the same 6,142 non-ASCII customers, the same 2,517 products, 74 stores and 4,018 dates spanning 2016-01-01 to 2026-12-31; only the fact tables grow. The customer pool does move at 10 M, to 1,679,846. `orders` falls short of its round number at every size — 93,470, 980,666, 9,887,613 — because the generator cuts orders outside its date window.
* **Deviation**: the smoke test that proves `sales` and `orderrows` hold the same rows is a 1:1 join, which at 23.7 M rows becomes a multi-minute nested loop of random primary-key lookups. The scaled sets make the same claim with five single-table aggregates that scan instead of joining, and they agree exactly. The core 100 k set keeps the join, where it is fast and is the stronger statement.
* **Creation**: the extended tier is nine datasets now — `adventureworks_dw`, `bts_ontime`, `chicago_crimes_full`, `contoso_1m`, `contoso_10m`, `dvdstore_reviews`, `nyc_taxi_yellow`, `wideworldimporters`, `wideworldimporters_dw`. The core set is unchanged at 21, and the image is unchanged.
* **Deviation**: two more object-counting smoke tests fixed before they could fail — `nyc_taxi` and `dvdstore` both counted views and routines in `information_schema` for their whole database, which breaks the moment an extended dataset adds one. Both now name the objects the core dataset owns. `megasamples/verify.py --pin` also no longer claims the core dataset's tables as an append dataset's own: which tables an append dataset owns is a declaration in its `expected_counts.yaml`, and pinning refreshes their values without adding to the list.
* **Verification**: X-02 complete. WideWorldImporters is the only dataset in the project with no script or CSV form — its transactional rows are generated by randomised T-SQL, so Microsoft's `.bak` is the only copy of the released data — and the maintainer accepted the SQL Server Developer EULA so it could be opened. **`wideworldimporters`: 48 tables, 4,713,833 rows, 633.7 MB, 98 foreign keys, 0 orphans. `wideworldimporters_dw`: 16 tables, 923,643 rows, 249.9 MB, 29 foreign keys, 0 orphans.** `SUM(extendedprice)` over the OLTP invoice lines is 198,043,439.45, the same number as the DW's `fact_sale` total — one restore, two databases ([OLTP](/datasets/wideworldimporters.md), [DW](/datasets/wideworldimporters-dw.md)).
* **Verification**: the export is byte-reproducible. Two independent restore-and-export runs produced **76 identical files** (74 table extracts plus the catalogue and the baselines), which required giving the 18 keyless `_Archive` tables a deterministic sort — SQL Server keeps their history in an index it manages, so they have no primary key to order by, and every sortable column together is enough.
* **Deviation**: four of thirteen `Purchasing.Suppliers` rows hold **a single NUL character as their address line** — not NULL, not an empty string. `bcp -c` writes NULL and `''` identically, so the export used a lone NUL to mean NULL, and read those four real values as NULL. Nothing failed; the per-column non-null counts caught it (9 parsed against 13 counted) before a row was loaded. Every exported field now carries a tag byte: `0x00` alone is NULL, `0x01` introduces a value of any content at all ([bcp record](/tools/sqlcmd-bcp.md)).
* **Deviation**: a lost row that no error mentioned. Every WWI DW dimension uses **key 0 for the "Unknown" member**; MySQL turns a 0 loaded into an AUTO_INCREMENT column into a generated value unless `NO_AUTO_VALUE_ON_ZERO` is set, so that row was renumbered onto key 1, collided with the real key-1 row, and `LOAD DATA LOCAL` — which implies `IGNORE`, because the server cannot stop a client mid-file — dropped it silently. `dimension_customer` loaded 402 of 403 rows. The fix is the sql_mode; the *reason it was visible* is that `expected_counts.yaml` for these two datasets is generated from SQL Server's `sys.partitions` and marked `# authority:`, which `megasamples/verify.py --pin` now refuses to overwrite. Pinning from the load would have recorded 402 as correct.
* **Deviation**: `9999-12-31 23:59:59.9999999`, which system versioning writes into every current row's `ValidTo`, **rounds up** into year 10000 on the way into `DATETIME(6)`, overflows, and lands as a zero date. The converter truncates explicitly instead and counts what it touched: 12,671,129 values in the OLTP database, 235,317 in the DW. The export keeps all seven digits; the loss is a MySQL-side decision, not a lossy export.
* **Deviation**: two bugs of my own, both silent. `"nvarchar".endswith("char")` is true, so every string column in both databases became a space-padded fixed-width `CHAR`. And the export reader dropped each row off the front of its buffer, which is quadratic in rows per read — minutes of pure copying on the 3.65 M-row archive.
* **Creation**: `megasamples/sources/mssql.py` (the container and the EULA gate — it refuses to start until acceptance is explicit, and prints the terms first), `megasamples/sources/wwi_export.py` (restore, catalogue, per-column baselines, `bcp` export) and `megasamples/sources/wwi.py` (the shared converter: types, temporal pairs, sequences to AUTO_INCREMENT, eight hand-translated computed columns, JSON, geography). Two of those computed columns needed a `COALESCE` the T-SQL does not have, because SQL Server's `concat()` renders NULL as an empty string while MySQL's `CONCAT` returns NULL — 199 of 227 stock items would have lost their `SearchDetails`.
* **Verification**: risk 6 retired and both WWI open questions closed by measurement. `bcp -c` under `LC_ALL=C.UTF-8` emits UTF-8 with no substitution (`Côte d'Ivoire`, `São Tomé and Principe`, `Türkiye` all intact through 4.7 M rows); bcp is 18.6.0002.1 and accepts `-u`; `-t 0x1f -r 0x1e0a` survive every value in 77 tables. Of the row counts the records held from memory, **every one of the OLTP's nineteen was exact**; four of the DW's fourteen were wrong, all because slowly-changing dimensions carry versioned rows the OLTP source does not ([row counts](/questions/mssql-wideworldimporters-row-counts.md), [bcp](/questions/mssql-bcp-linux-export-encoding-and-escaping.md)).
* **Deviation**: `docker pull mcr.microsoft.com/mssql/server` fails with a bare `EOF` while `curl -4` to the same URL answers in 0.15 s — the IPv6 signature again, and one the Docker Desktop fix applied at P-02 does not cover. Rather than ask for another privileged change, the runbook's own section-2 workaround became a script: `megasamples/pull_image.py` fetches manifest and blobs over IPv4, verifies every digest and hands Docker an OCI archive, pinning `image@sha256:` — a stricter fetch than `docker pull` of a floating tag ([runbook](/runbooks/ipv6-and-privileges.md), [container](/tools/mssql-server-container.md)).
* **Update**: the EULA acceptance is disclosed where an end user will meet it — `README.md`, the generated `NOTICE.md` and both `PROVENANCE.md` files now say plainly that using the published image involves no Microsoft licence, and that running `make wwi-export` does. `megasamples/provenance.py` grew a `build_license:` key for software used to *produce* a dataset that the image never contains ([licence](/licenses/microsoft-sql-server-developer-eula.md)).
* **Creation**: the extended tier has a mechanism now, not just a plan. `append: true` in a dataset's `dataset.yaml` means "add tables to a database the core build already made": `megasamples/engines/mysql/load.py` does not drop the database, and `megasamples/verify.py` scopes each side's checks to its own tables — an append dataset checks only what it added, and the core dataset ignores tables a declared append dataset owns. Both directions were proved against a database holding both tiers at once.
* **Verification**: X-03's dvdstore reviews are done — `make dvdstore-reviews` appends **200,000** reviews and **4,106,382** helpfulness rows to a loaded `dvdstore`, taking it from 16.3 MB to **475.9 MB** in 16 s. Both counts match the research exactly, every review's product and every helpfulness row's review resolves, and the star ratings are uniform at about 40,000 each ([record](/datasets/dell-dvd-store.md)).
* **Deviation**: a Python truthiness bug of mine, worth recording because it was silent and data-losing. The tier filter read `bool(touched) or (target and target.group(1) in EXTENDED_TABLES)`, which yields **`None`** when both parts are falsy — and `None != False` is true, so every statement matching neither test was skipped. It emptied the `categories` seed, which the pinned digests caught immediately.
* **Verification**: X-01 complete — AdventureWorks DW loads 30 tables and **1,047,467 rows** in 9.2 s at 161.2 MB, with 45 foreign keys and 0 orphans. Both figures the record could only infer are confirmed to the last digit: `SUM(salesamount)` is 29,358,677.2207 over `factinternetsales` and 80,450,596.9823 over `factresellersales` ([record](/datasets/adventureworks-dw.md)).
* **Deviation**: a missing space nearly cost the largest dimension. The install script writes `BULK INSERT[dbo].[DimCustomer]` — the only one of its 29 loads without whitespace after `INSERT` — so the reader skipped `DimCustomer` and its 18,484 rows in silence. It surfaced only because the converter reports every table it creates with no data file; nothing failed. The shared reader now treats that whitespace as optional.
* **Creation**: `megasamples/sources/bulkinsert.py` holds the BULK INSERT reader that both AdventureWorks converters now use — field-by-field reading, the `\n`-means-`\r\n` rule, and the NUL-is-empty-string rule. The OLTP converter was repointed at it and its output verified byte-identical to before the refactor.
* **Deviation**: four more T-SQL constructs handled, all general. `DATEDIFF(yy, a, b)` becomes `TIMESTAMPDIFF(YEAR, a, b)` and `DATEADD` becomes `DATE_ADD` — worth care because MySQL *has* a `DATEDIFF`, so the three-argument form would not always fail loudly. `CAST(x AS INTEGER)` becomes `CAST(x AS SIGNED)`. `TEXTIMAGE_ON` is stripped like the other filegroup clauses. And view names now join the identifier map alongside tables, because `vTimeSeries` selects `FROM vDMPrep` unquoted and MySQL is case-sensitive for view names too.
* **Verification**: the full local gate, run end to end exactly as PLAN.md §4.3 now specifies it — `make check` clean, `make core` green for all **21** datasets, `make image` bakes 22 databases, and `make test-image` passes every S8 check: ready in 2.3 s, registry matching the built set, 1,783 MB of data. This is what replaces a CI run for this project.
* **Verification**: R-01 complete, in the form the maintainer asked for rather than the form the plan assumed. **There is no CI service**: GitHub Actions was rejected because the pipeline downloads about 1.5 GB and bakes a 3.46 GB image, which is a poor use of hosted-runner minutes for a repository whose whole purpose is data. The two workflows that had been written were removed, and verification is a set of `make` targets running the same code on the build machine in Docker — `make check` before every commit, `make core-fast` plus `make image-only` and `make test-image` before a push, `make core` before a release. PLAN.md §4.3 and the [orchestration decision](/decisions/build-orchestration.md) are rewritten accordingly.
* **Creation**: `megasamples/provenance.py` writes `datasets/<name>/LICENSE` and `PROVENANCE.md` for all 21 datasets plus the root `LICENSES.md` and `NOTICE.md`, entirely from files already under version control, so the knowledge bundle stays the single source of truth and the generated files cannot quietly disagree with it. It is idempotent, and `--check` makes it part of `make check`.
* **Verification**: two datasets could never have run in a hosted CI regardless of budget, which is a property of the data rather than of the decision — `lahman` is maintainer-supplied with no fetchable URL, and `chicago_crimes` is a live API whose pinned digest changes daily. R-02's release assets are what will let anyone other than the maintainer verify those two.
* **Creation**: every artifact in `manifest.yaml` is now pinned by **both** length and content. `fetch.py` records `size_bytes` alongside the digest on a first fetch, and the 133 artifacts that had been pinned by digest alone were backfilled from the verified downloads.
* **Verification**: E-02 complete — on Employees' 3,919,015 rows, load-then-index is **1.28x** faster than declaring the keys with the tables (9.7 s against 12.3 s, best of three), and `megasamples/engines/mysql/bench.py` proves all three arms produced the same 9 indexes, the same row counts and the same 146.8 MB before it compares any times ([indexing strategy](/decisions/indexing-strategy.md)).
* **Verification**: the far larger finding is not about index order at all. Letting MySQL **validate** those six foreign keys afterwards costs **74.9 s — more than seven times the entire load** — while the pipeline's own S5 orphan stage checks the same six constraints as `LEFT JOIN ... IS NULL` counts in **1.4 s**, 53x faster, because by then the foreign key indexes exist and each check is an index lookup. That is the justification for loading with `foreign_key_checks=0` and testing referential integrity ourselves.
* **Deviation**: the benchmark's first run was measuring the wrong thing twice over, and both faults are worth remembering. It compared "no validation" against "validate six foreign keys", which made deferring look 6.6x *slower*; and it read `data_length` without `ANALYZE` first, which reported 31.6 MB against 146.8 MB for two databases holding identical data — the same stale-statistics trap that had just been found in the image test.
* **Creation**: a new requirement from the user — a browsing console. PLAN.md gains §12 and task **C-01**: an opt-in `console` Compose profile starting the image plus phpMyAdmin, Adminer, DbGate and CloudBeaver, each preconfigured against it, behind a landing page generated from the `megasamples.datasets` registry so it cannot drift from the image. Recorded as a [pending decision](/decisions/browsing-console-stack.md) with four [source records](/sources/docker-hub-phpmyadmin-image.md) and an [open question](/questions/console-preconfiguration-limits.md); not yet built.
* **Verification**: all four console images exist with pinnable version tags, and how far each can be preconfigured was read rather than assumed. phpMyAdmin and DbGate take server *and* credentials from environment and open straight into the data. **Adminer cannot**: its image documents no credential variable, so its login form stays and the landing page will show the credentials beside the link. **CloudBeaver is unresolved** — connections come from a mounted `data-sources.json`, but the wiki does not say whether a fresh container can start with no setup wizard and no hand-made administrator. The decision says a console that cannot start unattended is dropped rather than shipped with a manual step.
* **Verification**: E-01 complete — the image bakes **all 21 datasets plus the registry**: 22 databases, 249 base tables, **9,056,697 rows**, 942.8 MB logical, a **1,785 MB data directory**, a **3.46 GB image**, ready to serve a real query **2.2 s** after start against a 30 s budget. Every S8 check passes. PLAN.md section 7 and [tier-assignments](/decisions/tier-assignments.md) are rewritten with the measured figures; **no tier assignment changed**.
* **Deviation**: `megasamples/engines/mysql/image_test.py` was reporting the wrong size. It read `information_schema` right after startup, and a dump load leaves table statistics ungathered — it said 667 MB for an image whose data directory really holds 1,785 MB. It now measures the data directory, which is both accurate and the number a user pays for.
* **Verification**: logical size is not what the disk costs — the ratio across the image is 1.9x, and 3.4x for `stackexchange_beer`, whose FULLTEXT index over post bodies turns 21.5 MB of logical data into 74 MB on disk. Worth remembering when sizing anything with FULLTEXT.
* **Verification**: M-06 complete — wikipedia_simple builds the record's rule exactly: the 5,000 lowest `page_id` non-redirect articles in namespace 0 (ids 1..15,799), their text and the link rows among them. 9 tables, **160.9 MB** in InnoDB, 17.8 s, 12 smoke queries and 3 plan tests pinned. Every value the plan named as a test is confirmed — `page_id` 1 is `April` with `page_latest` 10861257 and `page_len` 22079, `ss_good_articles` is 284,761, `redirect.rd_from = 24` is `Catharism`, and revision and text are 5,000 each, one per page ([record](/datasets/wikipedia-simple.md)).
* **Deviation**: the sample measures **160.9 MB**, not the inferred "< 50 MB". It is content, not overhead — 69.5 MB of wikitext and 77 MB of link tables with their indexes — and it sits in the same band as employees, adventureworks and oracle_sh, so core still holds.
* **Verification**: the MediaWiki dumps need **no fix-up at all**, so `convert/fixup_sql.py` was never written. Each table's own `CREATE TABLE` is used verbatim and only the rows are filtered. Their `varbinary` columns hold bytes that are not valid UTF-8, so they are read and written with `surrogateescape` to reproduce the upstream bytes exactly.
* **Deviation**: a MediaWiki schema change the records predate — `categorylinks` no longer carries the category name in `cl_to` but points at `linktarget` through `cl_target_id`. The sample therefore has to keep the link targets of *both* link tables, and `v_category_member` joins through `linktarget`.
* **Deviation**: one bug of my own worth recording because it would have been silent: the generated file was assembled by substituting `SOURCE_PLACEHOLDER_<table>` markers, and `page` is a prefix of `pagelinks`, so the page dump was spliced into the middle of the pagelinks one. It failed loudly ("Table 'page' already exists"), but only by luck; the converter now reads each table's SQL straight into the output.
* **Verification**: M-06's enron is done — the core subset is 5 of 150 mailboxes under the record's 40 MB rule (`allen-p`, `arnold-j`, `arora-h`, `badeer-r`, `bailey-s`), **9,941 messages and 38,832 recipients**, 36.5 MB in InnoDB, 4.4 s, 12 smoke queries and 3 plan tests pinned ([record](/datasets/enron.md)).
* **Verification**: both Enron questions are answered and closed, from parsing **all 517,401 messages** rather than a sample. The corpus is far cleaner than the questions assumed: **0** bodies need a charset fallback, **0** messages lack or misformat a `Date`, and **0** `Message-ID` values repeat across 517,401 distinct ones; only 30 messages carry any header defect. Because that was measured, `message` now declares the `UNIQUE(message_id)` the plan had deferred. The tarball's own sha256 is pinned (CMU publishes none), and its 517,401 files in 150 mailboxes confirm both third-party counts.
* **Deviation**: the tar's member order is **not** alphabetical, so a subset rule that reads until full would pick the wrong mailboxes. The converter takes the member list in one streaming pass, chooses from it, and reads the chosen members in a second; the archive is never extracted.
* **Verification**: the [MediaWiki DDL question](/questions/mediawiki-sql-dump-ddl-compatibility-mysql-9-7.md)'s four worries are all unfounded on `mysql:9.7.2` — the MariaDB sandbox comment, `int(8) unsigned` display widths, `double unsigned`, `ROW_FORMAT=COMPRESSED KEY_BLOCK_SIZE=8`, `CHARSET=binary` with `varbinary`, and `timestamp DEFAULT current_timestamp() ON UPDATE current_timestamp()` are all accepted, and COMPRESSED is actually honoured rather than silently downgraded. The dumps need no filter.
* **Verification**: all eight Simple English Wikipedia files for dump run 20260901 downloaded and match Wikimedia's published md5 **and** byte size, including the 356,186,307-byte `pages-articles.xml.bz2`.
* **Verification**: M-04 complete — lahman builds from the CSV set the maintainer downloaded once: **27 tables, 706,466 rows, 77.7 MB** in InnoDB, 4.2 s, 12 smoke queries and 3 plan tests pinned. Career home-run leaders come out Bonds 762, Aaron 755, Ruth 714; seasons run 1871–2025; 19 distinct league ids, so the SABR-recognised Negro Leagues are there ([record](/datasets/lahman.md)).
* **Creation**: `manifest.yaml` gained `manual: true` and `megasamples/fetch.py` an artifact path that never touches the network — it verifies a maintainer-placed file against its sha256 and size, and when the file is absent it prints the source URL and the exact path to put it at. That answers the [Lahman question](/questions/lahman-box-direct-download.md) without putting 42 MB into git, and covers the other artifacts nothing can fetch, such as citibike and divvy behind their licence gate.
* **Deviation**: two of the record's expectations about the Lahman files were wrong in detail. BOMs are **not** universal — 4 of the 27 CSVs carry one (Parks, People, Schools, Teams) and 23 do not, so every file is decoded as `utf-8-sig`. And this copy's `readme2025.txt` is 47,976 bytes, md5 `cc2efbc580751222be1d18bb7df37006`, against the 47,982 / `56e80e2c…` recorded from the SABR page on 2026-09-02: the release has been revised in place, which is why the zip's own sha256 is what pins the build.
* **Verification**: Lahman's column types were measured rather than guessed — every value of every column was read and the narrowest type that holds them chosen. The result matches what the conversion-path decision predicted (`era` DECIMAL(4,2), `fp` DECIMAL(4,3), `yearID` SMALLINT), and all 18 declared primary keys were verified unique before being declared; none fell back to an index. The 2025 release also adds a surrogate `ID` column to People and Parks, which the record's schema notes did not mention.
* **Verification**: M-06's stackexchange_beer is done — 8 data tables plus 3 generated lookup tables, **62,492 rows**, 21.5 MB in InnoDB, 2.1 s, 11 smoke queries and 3 plan tests pinned. The **md5 archive.org publishes matches the download**, so unlike the live-API datasets this snapshot is fixed for good ([record](/datasets/stackexchange.md)).
* **Deviation**: the converter takes its column list from Stack Exchange's schema documentation rather than from the dump, because an attribute is optional and inferring from one site would define a narrower table than the format allows — and it fails if the data carries an attribute the documentation does not list. Measured, the two agree exactly: `EmailHash` and `ProfileImageUrl` never appear, as the record expected, and neither do `IsModeratorOnly`/`IsRequired`.
* **Verification**: 1,173 questions and 2,580 answers, 704 accepted, and every answer's parent question is present. Non-ASCII content confirmed in both directions (223 display names, 3,809 post bodies), and the per-row `ContentLicense` splits CC BY-SA 3.0 / 4.0 at 2,692 / 1,232 — the attribution requirement travels with each row, not just with a licence file.
* **Verification**: M-05 complete, and without the loader image P-04 was going to build — DuckDB reads the Parquet from the project venv instead. nyc_taxi loads **48,326** green trips and the full **265**-zone lookup in 0.7 s at 11.1 MB; chicago_crimes loads **259,268** crimes for 2024 and **434** IUCR codes in 3.3 s at 75.7 MB. Both have 10 smoke queries and 3 plan tests pinned ([nyc-tlc](/datasets/nyc-tlc.md), [chicago-crimes](/datasets/chicago-crimes.md)).
* **Verification**: the DOUBLE to DECIMAL(10,2) narrowing on the taxi money columns is lossless for this month — DuckDB's sums over the source Parquet and MySQL's over the loaded table agree to the cent on all three of total_amount, fare_amount and trip_distance. No trip carries a zone id outside the lookup either, so the two foreign keys onto `taxi_zone` are declared rather than left as bare indexes.
* **Verification**: the taxi archive's reputation for dirty data is measured rather than repeated — 3,447 of 48,326 trips are suspect by at least one test, and the pickup timestamps run from 2024-12-25 to 2025-02-05, 38 distinct dates in a 31-day month. They are kept, because the TLC states it did not create the data and makes no accuracy claim, and a `v_suspect_trips` view names them.
* **Verification**: the Chicago snapshot moved between research and build — 259,267 rows on 2026-09-02, 259,268 on 2026-09-03 with `X-SODA2-Truth-Last-Modified: Thu, 03 Sep 2026 11:00:44 GMT`. That is the documented behaviour and is exactly why the snapshot's identity is the digest, the row count and that header together rather than a checksum alone. Two conditionals are settled: `case_number` is not unique (259,239 distinct in 259,268 rows), and no crime carries an IUCR code missing from the lookup, so that foreign key is declared.
* **Deviation**: the City of Chicago's mandatory disclaimer is written into the generated SQL header **and** into the `crimes` table comment, so it travels with the schema rather than only with a licence file, and a smoke test asserts it is still there.
* **Verification**: M-04's contoso is done — 8 tables, **753,467 rows**, 103.3 MB in InnoDB, loading in 4.8 s, 7 foreign keys with 0 orphans, 10 smoke queries and 3 plan tests pinned. The CSV dialect the record could not document is measured: comma-delimited, one header row in DDL order, no quoting, ISO dates, empty field = NULL, UTF-8 with no BOM ([record](/datasets/contoso.md)).
* **Deviation**: two of the record's inferences are corrected by measurement. `orders` is **93,470**, not the generator's `OrdersCount` of 100,000 — the build cuts orders outside its date window — and the fact tables are 223,974 rows, a ratio of 2.40 rather than 2.43. Non-ASCII customer text is confirmed at 6,142 rows (`Stuttgart Dürrlewang`, `Mönchengladbach Großheide`).
* **Creation**: the build no longer needs a system 7-zip. `py7zr` reads the archive from the project venv, which `uv sync` now creates; the Makefile prefers `.venv/bin/python` when it exists. That also puts duckdb, lxml and sqlglot in place for the tasks that need them, and keeps the toolchain in `uv.lock` rather than in a machine's apt state.
* **Deviation**: three more upstream quirks handled in the T-SQL translator, each opt-in or bounded rather than global. SQLBI's DDL omits the comma before every table's PRIMARY KEY constraint, which is repaired behind a flag — pubs writes a column-level PRIMARY KEY on its own line, where the same repair would break the statement. SQL Server's index storage options (`WITH (PAD_INDEX = OFF, ...)`) are stripped, including the no-space `)WITH (` form. And `ALTER TABLE ... CHECK CONSTRAINT <name>` is skipped like the `ALL` form, with the table-name span matched loosely because Northwind's is `"Order Details"`.
* **Verification**: M-04's dvdstore is done — 9 tables, 174,716 rows, **16.3 MB** in InnoDB, loading in 1.7 s, 4 foreign keys with 0 orphans, 10 smoke queries and 3 plan tests pinned. All nine researched row counts reproduced exactly on the first load, and both hazards the record marked **Inferred** are confirmed: `LOAD DATA` does accept `2013/01/27` into a DATE column, and `00000` lands in `ZIP INT` as 0 ([record](/datasets/dell-dvd-store.md)).
* **Deviation**: the `RESTOCK` trigger is not created. Upstream ships it under the comment "Doesn't work yet!!!" and its body inserts hard-coded values regardless of the row, so there is nothing to port.
* **Verification**: the plan stage was too strict. A unique-index lookup on a constant is resolved before execution and the table then does not appear in the plan at all — the strongest access path there is, which the check was reading as a missing table. It now passes when the plan says the rows were fetched before execution, and still fails on a genuine typo.
* **Deviation**: M-04's other two datasets are blocked and need the user. contoso ships as `.7z` and the build machine has no 7-zip; lahman has no static URL — the inferred Box folder-zip endpoint returns `{"use_zpdl":"true","result":"failure"}`, confirming the [question's](/questions/lahman-box-direct-download.md) worry, so a one-off browser download stands.
* **Verification**: M-03's data half is complete — AdventureWorks OLTP loads 69 tables and **759,240 rows** in 14 s, 162 MB in InnoDB, with 91 foreign keys and 0 orphans, 14 smoke queries and 5 plan tests pinned. All 45 row counts verified during research reproduced exactly on the first load, and the 24 that were unknown are now measured ([record](/datasets/adventureworks-oltp.md)).
* **Verification**: the [row-count question](/questions/mssql-adventureworks-row-counts.md) is answered for OLTP and closed. Its second part had the premise backwards: `ProductReview.csv` really is 4 rows, and its comments really do contain newlines, because **BULK INSERT does not split a file into lines** — it reads field by field, and only the last field of a row ends at the row terminator. Reading the files that way is also what makes the 14 `+|`/`&|\n` tables work, and it replaced a line-splitting reader that had quietly turned 4 rows into 31.
* **Deviation**: two more details of that format had to be handled. A `ROWTERMINATOR` of `\n` means `\r\n` in the CRLF files, and a field holding a single NUL byte is the empty string rather than NULL — five fields in `Document.csv` depend on the second, including the root hierarchyid, which is an empty binary and not a missing value.
* **Deviation**: the 10 procedures, 11 functions and 8 triggers are **not** ported. Their T-SQL bodies need a routine translator that does not exist yet, and the converter names every object it skips rather than emitting something that parses but does not work. Six views are dropped for the same reason (four use XML `.nodes()`, one `PIVOT`), leaving 13 of 20 — including both views that read store and product XML through `ExtractValue`.
* **Verification**: the shared T-SQL translator gained nine fixes on this dataset, and two were silent-corruption bugs of the kind that do not announce themselves. `split_batches` counted apostrophes without skipping comments, so a line like `-- don't` left it believing it was inside a string and it found **12 batches where there are 434**. And `TYPE_MAP` had `hierarchyid` twice, the later entry winning, so every hierarchyid column became `VARCHAR(255)`; the dict now asserts it has no duplicate keys.
* **Deviation**: `convert_select_aliases` translates T-SQL's `Alias = expression` select-list form, which MySQL reads as a comparison. That was needed for AdventureWorks' views, and it also ported Northwind's `custordersdetail` procedure, which had been recorded as unportable since S-03.
* **Verification**: risk 5 is retired without SQL Server. `megasamples/sources/hierarchyid.py` decodes SQL Server's hierarchyid binary and encodes it back; over all 290 AdventureWorks employees every decoded path has exactly the level the `OrganizationLevel` column states, every path re-encodes to its original bytes, all 290 paths are distinct and every node's parent path exists. The code table was derived from the data — one manager has 22 consecutive children, which pins values 1 to 22 — and agrees with the two values Microsoft documents. Codes outside the verified ranges raise rather than guess ([probe](/sources/mysql-9-7-srid-4326-axis-order-probe.md)).
* **Verification**: geography decodes as SRID 4326 plus latitude then longitude; row 1 of Address.csv is 47.786992, -122.164645, which is the Bothell address the row gives. MySQL's own axis order for SRID 4326 was measured rather than assumed: `POINT(x, y)` takes longitude then latitude, and the SRS axis order then makes `ST_AsText` print latitude first. Passing latitude first is rejected outright once it exceeds ±90. This closes an item both the OE and AdventureWorks LT records had left unverified.
* **Verification**: M-02 complete — Oracle Sales History builds and verifies green: 9 tables, **1,063,284 rows** loaded from 91 MB of CSV in 10.5 s, 168.7 MB in InnoDB (the record inferred 150–250 MB, so core-medium stands), 10 foreign keys with 0 orphans, 15 smoke queries and 5 plan tests pinned. Every count `sh_install.sql` publishes for itself was reproduced exactly ([SH record](/datasets/oracle-sh.md)).
* **Verification**: the [SH padding question](/questions/oracle-sh-csv-row-counts-and-padding.md) is answered and closed, all six parts. The most useful answers were the two that contradicted an assumption: MySQL loads `1232.16` followed by 47 spaces into `DECIMAL(10,2)` with no warning at all, so the padding pre-pass is not strictly needed (it is still done, and measurably touches only the 918,843 padded fields); and `''` → NULL is needed far more widely than the two columns the question named, including in the script's own INSERTs where it lands in integer columns and MySQL rejects it outright.
* **Verification**: partitioning versus foreign keys was confirmed on the target server rather than taken from the manual — `ERROR 1506 (HY000): Foreign keys are not yet supported in conjunction with partitioning`. 15 partitions on `sales` and 20 on `costs` are dropped, with each table's MySQL COMMENT saying so.
* **Verification**: mapping SH's DATE columns to `DATE` rather than `DATETIME` was checked before it was relied on: scanning all six CSVs for a date carrying a time finds zero matches in 1.06 M rows. The shared translator keeps DATETIME as its default; SH passes the choice in.
* **Creation**: the Oracle path gained the constructs SH needed, all of them general — range/list partitioning stripped, bitmap indexes as ordinary B-trees, `CREATE MATERIALIZED VIEW` as a plain view, `CREATE DIMENSION` dropped, an Oracle Text `INDEXTYPE IS ctxsys.context` index as InnoDB FULLTEXT, storage clauses (`LOCAL NOLOGGING COMPRESS ...`) stripped repeatedly rather than in an assumed order, `MODIFY CONSTRAINT ... DISABLE` recognised as choreography, `owner.` prefixes stripped, indexes on materialized views dropped, and foreign key column types aligned to the key they reference.
* **Deviation**: Oracle column comments are now attached to their columns instead of being dropped — 81 of 81 for SH, 35 of 35 for HR, 43 of 67 for CO (the other 24 name view columns, which MySQL cannot comment). Doing so exposed a latent defect: `inline_identity_pk` searched the whole statement for "PRIMARY KEY", and Oracle's own comments say things like "primary key column", so an attached comment made it skip a table that had no key at all. It now blanks string literals before searching.
* **Verification**: the image bakes all eleven core-fast databases and passes every S8 check, ready in 1.4 s against a 30 s budget, 160 MB of user data in a 1.87 GB image. The new risk the cross-database keys introduced — that each dataset is loaded into the image from its own dump, so a key into another database could be resolved away — does not materialise: all three survive with 0 orphans, and `megasamples/engines/mysql/image_test.py` now checks for them so it stays that way.
* **Verification**: S-06 complete — Oracle Order Entry builds and verifies green: 9 tables, 11,518 rows, 10 foreign keys (3 into `oracle_hr`) with 0 orphans, 19 smoke queries and 5 plan tests pinned, 4.4 MB loaded in 1.2 s. Every researched count and probe held, including all four values of order 2458 and both encoding canaries — `LCDモニター11/PM` from `UNISTR('LCD\30e2\30cb\30bf\30fc11/PM')` and the German `Auflösung für optimale Bildqualität` ([OE record](/datasets/oracle-oe-pm-ix.md)).
* **Verification**: the one thing in OE that could not be inferred was settled by measurement. Every OE script wraps its lines with SQL*Plus's `-` continuation — 76,831 of them, *inside string literals* — so the join rule decides what 11,140 rows of text say, and not one continuation has a space before the hyphen. The [SQL*Plus guide](/sources/sqlplus-user-guide-continuation-character.md) says the lines are joined and the hyphen removed but not what replaces it; joining with nothing would run 264 pairs of words together in the English descriptions alone. [An independent export](/sources/oe-independent-export-sql-problems.md) settles it at one space, and 283 of 288 descriptions then reassemble byte for byte, the five remainders being that copy's own typographic edits.
* **Deviation**: OE's object-relational parts are flattened as designed — the address object into five columns, the phone VARRAY into a 378-row child table, both SDO_GEOMETRY points into longitude/latitude, and the XMLType warehouse spec into the raw document plus its eight elements, with `ExtractValue` still working on the document. All 8 views port, the `insert_ord_line` trigger ports, all 43 upstream comments are attached inline, and `get_phone_number_f` and the 6 HR synonyms are dropped with reasons. The converter checks the scripts' 18 objects and 20 indexes against an explicit inventory.
* **Verification**: the index list in the record came from `oe_idx.sql`, which the installer never calls. It runs `cidx_v3.sql`, where `inv_warehouse_ix` is commented out — 13 named indexes, not 14. Three more files in that directory (`oe_p_cus`, `oe_p_ord`, `oe_p_whs`) are legacy near-duplicates that nothing reaches either.
* **Deviation**: three defects of the same kind — a filter that quietly ate real content. `SET sales_rep_id = NULL` on its own line was being dropped as a SQL*Plus `SET` directive, taking the whole "online orders have no sales rep" update with it; `DEFINE` and `--` lines were not being dropped at all, so each absorbed the following statement. All three were caught because the converter refuses to skip a statement it cannot parse, and each was confirmed by a row count that came back one short.
* **Deviation**: keeping the three foreign keys into `oracle_hr` means `oracle_hr` can no longer be dropped on its own, and dropping with the checks off is worse — MySQL re-resolves the dangling key while the recreated table still has no unique index and the reload fails. `megasamples/engines/mysql/load.py` now drops inbound foreign keys first and names the datasets to reload; `dataset.yaml` gained `depends:`. `megasamples/verify.py` also had to read `referenced_table_schema` rather than assume the referenced table is in the same database.
* **Verification**: S-07 complete — AdventureWorks LT builds and verifies green: 12 tables, 4,150 rows, 12 foreign keys with 0 orphans, 13 smoke queries and 4 plan tests pinned, 4.1 MB loaded. The three computed columns were checked the hard way rather than trusted: all 574 values MySQL generates were compared against the values the data files ship, and every one matches ([AdventureWorks LT](/datasets/adventureworks-lt.md)).
* **Deviation**: `vProductModelCatalogDescription` was researched as "stub/drop" because it queries XML with XQuery. It ports after all. MySQL's XPath subset has no namespace support and rejects `local-name()`, but it matches a prefixed element name literally, and the catalog documents use exactly the prefixes the view's queries declare, so `col.value('...')` becomes `ExtractValue(col, '...')` with the `declare namespace` preamble dropped. All three views now work.
* **Deviation**: `SalesOrderHeader.SalesOrderNumber` is an ordinary column, not a generated one. MySQL rejects a generated column that reads an `AUTO_INCREMENT` column, and keeping the identity on `SalesOrderID` is worth more than keeping the column generated; the data file carries the same `'SO' + SalesOrderID` value. The two computed columns that do not touch the identity are `GENERATED ... STORED` as researched.
* **Deviation**: the two T-SQL error-handling procedures and the two table-valued functions are dropped rather than stubbed, and the two triggers are hand-written as MySQL row triggers in `datasets/adventureworks_lt/objects.sql` — one T-SQL trigger covers three events, and an AFTER trigger may not update its own table. The converter checks the upstream script's ten programmable objects against an explicit inventory and fails the build if it changes, so nothing can go missing quietly.
* **Verification**: the shared T-SQL translator was silently dropping every `CREATE CLUSTERED INDEX` and `CREATE NONCLUSTERED INDEX` statement, because those keywords stopped the statement matching the index filter. pubs was therefore missing all seven of the indexes its script creates, and the record only expected two of them. Found by generalising the keyword strip for AdventureWorks LT and then re-running the other datasets ([pubs](/datasets/pubs.md)).
* **Verification**: re-pinning pubs exposed a second latent defect of our own — `authors_with_most_titles` ordered by last name only, and two authors named Ringer tie, so the answer depended on the query plan. The new indexes changed the plan and the test caught it. Every smoke query using `LIMIT` was audited for a total ordering; this was the only one at risk.
* **Verification**: `Mart¡nez` in Customer 205 is upstream's own data, not a conversion fault: the byte is `0xA1`, which is `¡` in the Windows-1252 code page the script itself specifies. SQL Server produces the same string. Reproduced rather than corrected.
* **Deviation**: `SELECT SUM(totaldue) FROM salesorderheader` measures **956,303.5949**. The record carried 1,124,180.4489 marked **Inferred**; that is the full AdventureWorks OLTP figure, not LT. Corrected in the record.

## 2026-09-02
* **Verification**: M-01 complete — Employees loads 3,919,015 rows in 15.2 s and measures **146.8 MB** in InnoDB (the record inferred 150–250 MB). The upstream `test_employees_sha2.sql` reports `CRC OK` and `count OK` for all six tables, matching the SHA-256 values recorded during research: independent confirmation that the conversion is byte-perfect ([Employees](/datasets/employees.md)).
* **Verification**: all eleven Employees artifacts match the sizes recorded during research, totalling 172,285,719 bytes of dumps exactly as the record states.
* **Verification**: S-05 complete — smallsets, jaffle_shop, oracle_hr and oracle_co all build and verify green, bringing the core set to eight datasets. `make image` now bakes all eight and `make test-image` still passes every check. Every record prediction held again: Titanic 500 survivors and 263 missing ages, iris 50/50/50 with rows 35 and 38 carrying the **corrected** values that justified sourcing from scikit-learn, penguins 152/124/68, HR employee 100 and its 35 commissioned employees, and CO's `2021-02-04 13:20:22.245676` timestamp truncation.
* **Creation**: `megasamples/sources/plsql.py` and `megasamples/sources/oracle_convert.py` translate the Oracle sample schemas: SQL*Plus directives, `/`-terminated PL/SQL units, mask-driven `TO_DATE`/`TO_TIMESTAMP` conversion, `NUMBER`/`VARCHAR2`/`DATE` type mapping, identity columns, Oracle's parenthesised `ALTER TABLE ADD (...)`, `REFERENCES` without a column list, `LISTAGG`, and constant PL/SQL variables. `megasamples/sources/csvtable.py` turns a CSV into DDL plus inserts from an explicit column specification.
* **Verification**: three defects that would each have silently lost or corrupted data. The populate scripts wrap their inserts in an anonymous `BEGIN` block, and stripping that wrapper before the `REM`/`Prompt` directives silently dropped **the first row of every table**. `UTL_RAW.CAST_TO_RAW(...)` had its opening call stripped but not its closing paren. And one CO product's JSON exceeds SQL*Plus's line limit, so it is assigned to a PL/SQL variable with `|| chr(10) ||` and had to be inlined or the row could not be written at all.
* **Deviation**: scikit-learn's `iris.csv` uses its "bunch" layout — the first line is `(n_samples, n_features, target names)`, not column headers, and the species is a 0/1/2 target index. The converter declares the columns positionally and maps the index back to the species name ([iris](/datasets/iris.md)).
* **Verification**: S-04 complete — `make image` builds `sql-megasamples-mysql:dev` with Sakila, Chinook, Northwind and pubs baked in, and `make test-image` passes all 27 S8 checks: ready in 1.8 s, no re-initialisation, registry matches the built set, 51 tables verified with `CHECK TABLE` clean, `demo` read-only including through a routine, no empty passwords, and the password-override wrapper working in both directions ([bake decision](/decisions/bake-data-vs-initdb.md)).
* **Verification**: two predicted requirements bit on the first build attempt and confirm the tool records. `mysqlsh` without `--no-password` prompts, and a prompt in a non-interactive `docker build` hangs forever instead of failing — the second review flagged exactly this and PLAN.md carried the flag, but the Dockerfile did not. `util.loadDump` needs `local_infile=ON` on the target, as [the MySQL Shell record](/tools/mysql-shell-utilities.md) states.
* **Creation**: `megasamples/engines/mysql/dump.py` (MySQL Shell dumps, content-addressed), `megasamples/registry.py` (the single writer of `megasamples.datasets`, built from dataset.yaml, the download metadata, the dump manifest and the pinned counts), `engines/mysql/Dockerfile`, `engines/mysql/entrypoint-wrapper.sh`, `megasamples/engines/mysql/image_test.py`.
* **Verification**: S-03 complete — Northwind and pubs both build and verify green through the shared T-SQL translator (`megasamples/sources/tsql.py`). Northwind: all 13 counts, 3,308 INSERTs, 16 views, 13 foreign keys, and every probe in the record confirmed including the order total 1,265,793.04 and `LENGTH(picture)` 10746. pubs: all 11 counts, 255 rows, `SUM(qty)` 493, and the `LIKE '[0-9]…'` CHECK converted to a REGEXP that all 23 rows satisfy.
* **Verification**: two fidelity bugs that would have silently produced a wrong database were found by loading rather than by reading. SQL Server's `tinyint` is unsigned 0–255 while MySQL's is signed, so `pubs.jobs.min_lvl` was rejected until the mapping was corrected; and MySQL parses inline `REFERENCES` and then ignores it, so pubs — which declares all ten of its foreign keys inline and has no `ALTER TABLE` — would have ended up with no foreign keys at all. They are now hoisted to table-level constraints and validate with 0 orphans ([conversion path](/decisions/mssql-northwind-pubs-conversion-path.md)).
* **Verification**: the S4 digest caught real non-determinism in `pubs.titles`: two rows omit `pubdate`, so `DEFAULT GETDATE()` stamps them with the build time. Faithful to SQL Server, so the column is excluded from the digest with the reason recorded in `datasets/pubs/tests/digest_exclude.yaml` and covered by a smoke query instead ([pubs](/datasets/pubs.md)).
* **Deviation**: three objects are not ported and are emitted as commented text naming the construct that blocks them — Northwind's `custordersdetail` and `salesbycategory` (T-SQL select-list aliasing and variable assignment) and pubs' `employee_insupd` trigger (statement-level, reads the `inserted`/`deleted` pseudo-tables). The converter now fails the build if any upstream object is neither emitted nor reported, because a silently dropped trigger does not break the load, it just quietly makes the database less faithful.
* **Verification**: S-02 complete — `make chinook` green on the first attempt. All 11 researched row counts matched, 11 foreign keys with 0 orphans, every column `utf8mb4` with no surviving `NVARCHAR`, 84 identifiers lower-cased into `name_map.yaml`, and the accent/curly-apostrophe/slashed-date probes all correct ([Chinook](/datasets/chinook.md)). `make core-fast` rebuilds both datasets from an empty server and reproduces every pinned fingerprint.
* **Creation**: `datasets/chinook/convert.py` rewrites the upstream script with a scanner that tracks string and comment context rather than regex substitution, because the data contains values ending in `N` immediately before their closing quote — a blind `N'` → `'` replacement would silently corrupt them.
* **Deviation**: the build server mounts `build/stage` read-only and leaves `secure_file_priv` at its default. Pointing `secure_file_priv` at the context directory made the entrypoint chown it to uid 999 on every start, which took the directory away from the host user and broke staging. Files needed for server-side `LOAD DATA` will be copied into `/var/lib/mysql-files` instead.
* **Verification**: S-01 complete — `make sakila` runs fetch, stage, load and all six test stages green. Every one of the 16 row counts recorded in [Sakila](/datasets/sakila.md) matched a real MySQL load on the first attempt; 22 foreign keys, 0 orphans; 42 indexes including the SPATIAL one; smoke results pinned. Reproducibility confirmed by destroying the build server and reloading from scratch: all 16 per-table fingerprints reproduced identically.
* **Creation**: the shared pipeline — `megasamples/engines/mysql/server.py` (build-server lifecycle), `megasamples/canon.py` (the canonical digest, generating SQL and computing the Python side from one definition), `megasamples/verify.py` (stages S3–S7), `megasamples/stage.py` (host-side extraction, because the base image has no unzip), `megasamples/engines/mysql/load.py`, and a Makefile that is a thin shim over them so the pipeline stays testable without make.
* **Deviation**: the NULL sentinel in the canonical row text is `CHAR(0)` rather than the `\N` the checksum decision first specified. MySQL strips the backslash before an unrecognised escape, so `'\N'` in a SQL literal is the single character `N` and would collide with a real value of `N` — the same class of trap as the `\x1f` separator found at P-02 ([checksum method](/decisions/test-checksum-method.md)).
* **Deviation**: the build server mounts its `secure_file_priv` directory read-write, because the official entrypoint chowns that directory at start and fails on a read-only mount.
* **Verification**: P-03 bake test passed with Sakila. The entrypoint takes the already-initialized branch, `COPY --chown` preserves uid 999, first start is 1.53 s and restart 1.27 s against a 30 s budget, data and `CHECK TABLE` are exact, and `demo` is read-only over TCP. Design A (keep `/var/lib/mysql`) is confirmed and the design-B fallback is not needed ([bake decision](/decisions/bake-data-vs-initdb.md)).
* **Deviation**: three defects found at P-03 and fixed in the plan. `mysqld --initialize-insecure` leaves root@localhost passwordless and the entrypoint never fixes it on a baked directory, so `00-users.sql` sets it; consequently the account script must run last in the builder, which the plan's §2.2 snippet did not do; and the base image has no `unzip` or `python3`, so all extraction happens on the host.
* **Verification**: the baked data directory is 235 MB with only Sakila loaded, about 100 MB of it the default redo-log capacity. The size budget in PLAN.md §7 must read *base + ~200 MB fixed + data*; E-01 will re-measure.
* **Verification**: P-02 on `mysql:9.7.2` (pulled after the user fixed Docker Desktop's IPv6 path; 9.7.3 still unpublished so the pin stands). Every assumed server default confirmed, and three open items answered: `@@explain_format` is `TREE` by default, `GROUPING SETS` is HeatWave-only (`ERROR 3889`) so Oracle CO's view uses `ROLLUP`, and `CAST('2021/1/1' AS DATETIME)` works under the strict default. `MD5`/`SHA1` confirmed absent. Details in [MySQL 9.x notes](/tools/mysql-9x-behaviour-notes.md), [Oracle CO](/datasets/oracle-co.md).
* **Verification**: the canonical row digest was validated across the language boundary — SQL `SHA2(CONCAT('a',CHAR(31),'b'),256)` reduced through `CONV`/`CAST` equals Python's `hashlib.sha256` value exactly (`17315457580335015581`). The uncast `SUM` loses precision as the first review found (`1.8446744073709552e19` vs `18446744073709551620`) and `SUM` over an empty table is NULL as the third review found. A new trap was found and recorded: a `'\x1f'` escape inside a MySQL string literal is the text `x1f`, so the separator must be `CHAR(31)` ([checksum method](/decisions/test-checksum-method.md)).
* **Verification**: `mysqladmin ping` reports success on access-denied, so it cannot gate the image test; S8 waits for a real statement ([bake decision](/decisions/bake-data-vs-initdb.md)).
* **Verification**: `python3 -m megasamples fetch sakila chinook` — both artifacts downloaded and matched the sha256 values recorded during research exactly (sakila-db.zip 729,654 B `e27ed871…`; Chinook_MySql.sql 616,450 B `02a18354…`), confirming the pinned digests in [Sakila](/datasets/sakila.md) and [Chinook](/datasets/chinook.md).
* **Verification**: `MEGASAMPLES_TRUST_FIRST_FETCH=1 python3 -m megasamples fetch northwind pubs` — first fetch pinned sha256 `3cc62b3f…` (1,049,720 B) for [Northwind](/datasets/northwind.md) and `c66479d4…` (125,718 B) for [pubs](/datasets/pubs.md) into `manifest.yaml`.
* **Verification**: research claims checked against the downloaded bytes — Sakila inner-file sizes/md5s, version, encoding and object counts; all 13 Northwind row counts and the 3,308 INSERT total; all 11 Chinook row counts, CRLF endings and the 585 non-ASCII lines. Every claim matched; the three apparent mismatches were defects in the checking probes (a regex matching the `insert` inside `identity_insert`, statement-counting against multi-row inserts, and a heredoc-escaped byte literal), now recorded in the affected records so the executor's own tests avoid them.
* **Deviation**: PLAN.md task P-00 asked for `make jq zstd bzip2 p7zip-full` on the host; only `make` is actually required. Python's stdlib covers bz2 and zip, MySQL Shell handles zstd inside the image, and 7z belongs in the loader image, so the other four were not requested ([build orchestration](/decisions/build-orchestration.md)).
* **Verification**: `docker pull` is blocked on this machine for any image beyond a few KB (EOF from `production.cloudfront.docker.com`); from WSL, IPv4 fetches a 159 MB layer at 116 MB/s while IPv6 fails instantly, so the Docker Desktop VM is attempting IPv6. Handed to the user as a privileged change per [executor discipline](/runbooks/executor-discipline.md); P-02's in-container checks are blocked until it is applied ([environment survey](/sources/build-machine-environment-2026-09-02.md)).
* **Update**: Third review pass (partial: the coordinator was rate-limited, six verifiers and one finder returned): `megasamples/okf_fix_quotes.py` rewritten after it was shown to rewrite authors' tokens (`version: 1.10` became `"1.1"`); it now splices quotes into the raw frontmatter at the composer's marks and has a real CLI. `megasamples/okf_check.py` gained a CommonMark fence state machine, stale `# Applied to` detection, a cached link index and a stale-directory fix. Dispositions in [review dispositions](/decisions/review-2026-09-02-dispositions.md).
* **Update**: Reserved-word rule added to the [naming convention](/decisions/database-naming-convention.md) and the wrong statement in [Lahman](/datasets/lahman.md) corrected (`RANK` is reserved since MySQL 8.0.2); [Enron](/datasets/enron.md) view switched from `GROUP_CONCAT` to `JSON_ARRAYAGG`; five one-way license references given their back-links; the Lahman 404 probe dropped from `sources` and the probe rule stated in [the conventions](/runbooks/knowledge-bundle-conventions.md).
* **Update**: Second review pass (15 findings, ~30 sub-findings, 6 Copilot comments): dispositions appended to [review dispositions](/decisions/review-2026-09-02-dispositions.md). Root account and time-zone data added to the builder ([bake decision](/decisions/bake-data-vs-initdb.md), [accounts](/decisions/database-naming-convention.md)); one loader rule ([large-tabular path](/decisions/large-tabular-conversion-path.md)); `COALESCE` in the [checksum method](/decisions/test-checksum-method.md); Oracle NUMBER mapping restricted ([fixed-point source](/sources/mysql-refman-9-7-fixed-point-types.md), [9.x notes](/tools/mysql-9x-behaviour-notes.md)); sqlparse rejected; eight records' test SQL lower-cased; seven license Applied-to gaps filled.
* **Update**: `megasamples/okf_check.py` hardened (type-checked frontmatter, code-fence-aware headings, Inferred-section hedge gate, Status/deprecated rules, license coverage for Dataset/Tool records, write-then-validate indexes); `megasamples/okf_fix_quotes.py` replaced by a YAML-aware canonicalizer and the whole bundle canonicalized to block-style frontmatter.
* **Update**: Conventions runbook revision 2 after the first code review: trust rule rewritten (verified records may carry marked inferences), source granularity defined per artifact, required sections per type, tier vocabulary, log verbs, bundle_status marker ([conventions](/runbooks/knowledge-bundle-conventions.md), [dispositions](/decisions/review-2026-09-02-dispositions.md)).
* **Update**: `megasamples/okf_check.py` rewritten (PyYAML required; sections, tags, links in every file, log verbs, index content, decision status consistency, hedge scan); `megasamples/okf_fix_quotes.py` rewritten (parser-driven, guarded); all `accessed`/`version`/timestamp scalars quoted across the bundle.
* **Update**: [checksum method](/decisions/test-checksum-method.md) digest cast to UNSIGNED, floats and JSON excluded from the digest, native-SQL baseline exception; [bake decision](/decisions/bake-data-vs-initdb.md), [naming convention](/decisions/database-naming-convention.md), [large-tabular path](/decisions/large-tabular-conversion-path.md), [repository layout](/decisions/repository-layout.md), [schema mapping](/decisions/schema-to-database-mapping.md), [jaffle path](/decisions/jaffle-shop-conversion-path.md) aligned with PLAN.md (single --init-file wrapper, megasamples/registry.py, no initdb scripts, jaffle_shop_gen, column underscores).
* **Update**: all 34 dataset records re-tagged with the tier vocabulary; test SQL lower-cased in seven records; init-path wording removed from sakila, employees, dvdstore records and decisions; from-memory claims in oracle-hr, oracle-oe, oracle-sh replaced by links to verified records; AdventureWorks OLTP tier section rewritten to core (medium).
* **Update**: 24 license records normalized to the License template with a `# Attribution` section each; Applied-to gaps filled (Stack Exchange under CC BY-SA 3.0, Wikidata under CC0, TPC-C under Apache-2.0); 15 tool records, 12 question records and 40 source records received the missing template sections; 7 decision records received Options/Evidence/Outcome sections.
* **Update**: nine dataset records edited earlier today for naming/tier alignment (chinook, contoso, dell-dvd-store, employees, iris, jaffle-shop, palmer-penguins, titanic, chinook-conversion-path) carry a `generated.at` later than their `verified.at`; the load-bearing claims were not re-checked, which is why `verified` was not re-stamped.
* **Creation**: [review dispositions](/decisions/review-2026-09-02-dispositions.md).
* **Creation**: `PLAN.md` written (sections 1–11) from the bundle; every dataset subsection links to its record; open questions carried into §9.
* **Creation**: [tier assignments](/decisions/tier-assignments.md), [python-conversion-stack](/tools/python-conversion-stack.md), [docker-build-multistage](/tools/docker-build-multistage.md); PyPI source records for PyMySQL, sqlglot, pyarrow, lxml.
* **Creation**: Created the bundle (OKF v0.2) with directories `datasets/`, `licenses/`, `tools/`, `decisions/`, `sources/`, `runbooks/`, `questions/`; conventions in [knowledge-bundle-conventions](/runbooks/knowledge-bundle-conventions.md).
* **Creation**: [OKF spec v0.2 source](/sources/okf-spec-v0-2.md), [okf.md guide](/sources/okf-md-annotated-guide.md), [Google Cloud announcement](/sources/google-cloud-okf-announcement.md).
* **Creation**: MySQL version evidence ([9.7 releases page](/sources/mysql-refman-9-7-releases.md), [9.7 notes](/sources/mysql-9-7-release-notes.md), [8.4 notes](/sources/mysql-8-4-release-notes.md), [image versions.json](/sources/docker-library-mysql-versions-json.md), [image README](/sources/docker-library-mysql-readme.md)) and the [target version decision](/decisions/target-mysql-version.md) (9.7 LTS).
* **Verification**: build machine surveyed read-only ([environment record](/sources/build-machine-environment-2026-09-02.md)); `docker manifest inspect` shows `mysql:9.7.2` present and `9.7.3` absent → pin 9.7.2.
* **Creation**: architecture decisions — [naming and accounts](/decisions/database-naming-convention.md), [tier model](/decisions/tier-model.md), [repository layout and manifest](/decisions/repository-layout.md), [build orchestration](/decisions/build-orchestration.md), [bake data vs initdb](/decisions/bake-data-vs-initdb.md), [checksum test method](/decisions/test-checksum-method.md), [indexing strategy](/decisions/indexing-strategy.md), [schema-to-database mapping](/decisions/schema-to-database-mapping.md), [tier assignments](/decisions/tier-assignments.md).
* **Creation**: 34 dataset records, 24 license records, 28 tool records, 37 open questions and 368 source records written by seven parallel research agents (SQL Server, Oracle, MySQL-native/small, benchmarks, public tabular, text corpora, tooling/platform); every source record names the URL and access date 2026-09-02.
* **Creation**: runbooks [executor-discipline](/runbooks/executor-discipline.md) and [ipv6-and-privileges](/runbooks/ipv6-and-privileges.md).
* **Update**: [bake-data decision](/decisions/bake-data-vs-initdb.md) upgraded to verified after reading the official entrypoint source; remaining measurement is task P-03 in PLAN.md.
* **Update**: coordinator resolved pending decisions: [schema mapping](/decisions/schema-to-database-mapping.md) (option A lower-cased), [Oracle path](/decisions/oracle-conversion-path.md) (Python converters), [Wikidata vs Simple English Wikipedia](/decisions/wikidata-vs-wikipedia-simple.md) (Wikipedia).
* **Creation**: `megasamples/okf_check.py` (conformance, trust/verified pairing, links, index coverage, log order) and `megasamples/okf_fix_quotes.py`; indexes generated with `--write-index`.
